# PROJECT_STATE

快照：2026-09-12_10-23-02 UTC。仅时间戳最新一份有效，历史快照/任务原样保留。

## 本次交付核验增量
本次仅修复交付组织，不升级运行时版本。可完整恢复的应用仍为 R16 `ded624dda9344092d79dc5663cd3fa037145ec2a`；R17 在当前输入中仅有截图，没有源码、Git提交、wheel或ZIP，不能从此前聊天的实测数字重建或宣称已交付。
R1至R16等当前31个原始ZIP已重新CRC/路径/嵌套归档核验，无字体文件。R16原包SHA256与原receipt匹配；1712个文件、263个wheel运行文件、803提交、Git/fsck/阶段继承与R2补丁重放本轮复验通过。
首次Git恢复因当前沙箱overlay文件系统fsync返回EIO失败，/tmp与/mnt/data最小复现均失败，/dev/shm正常；仅更换临时目录后运行未修改的VERIFY_PACKAGE.py --git通过，未禁用fsync、未修改原包。
新全量交付包APP/保留原R16完整目录原字节，新上手包APP/保留原R16离线工具原字节。外围只增加入口、示例、说明、交付校验和状态，不伪称R17。精确外层ZIP哈希见本轮下载清单。
本轮未全量回归、未复核模型榜单、未调用商业API；Windows原生file://双击依旧未经实机验收。下载文件存在和校验成功不等于已保存到用户电脑，也不保证会话附件永久在线。
已知R16运行时__version__仍为r15而PACKAGE/DELIVERY_VERSION/wheel元数据为r16；本轮保留原版未改代码，不宣称此问题已修复。

## 核心目标与阶段
用户要求从实际使用出发直接改善全流程，不只增加模块。已有长期诉求：只用顶尖模型、经常借助外部工具、可真实下载并在自己设备保存是必要交付。
当前R16，继承真实R15 `a45e34214feaadb6cc8a7bcb707f8abefc55a06e`；原ZIP SHA256 `312823ef257c2044ca7873fbad19f8984eff5bae261ad671002f06196b300a7b`。R3/R4为功能重建，不是原包字节恢复。最终交付提交与校验数量只读PACKAGE/EVIDENCE/独立receipt，不从聊天和版本名推断成功。

## 有效决定及理由
本轮不新增编号工作区或新模型名单。首页统一文件识别后交给原入口，仍需选择用途、核验、预览和确认，避免记忆各区入口。媒体扩展名只提出用途，不作为内容有效证明；不认识的材料明确拒绝。主影片包、合集、外部交换ZIP提示先解压，不伪装通用转换。
新Desk/v1外壳含原字节STUDIO.zip及DESK.json/MANIFEST.json，保留原Studio协议与旧版本读内层能力。载入未接受的外部改稿、已加载个人模板、模板名/说明/副本名原始输入一起保存；恢复后仍待应用，不恢复当前型号选择或批准。跨区草稿/缓冲变化令预览失效。
下载发起与实际选回核验分开。两者均不能推导浏览器已永久保存或文件已到用户电脑。核验当前内容后才消除对应提醒；beforeunload仅辅助，不能当自动保存。保留旧单区和三工作区保存入口，不强迫迁移。
无效替换保留上一次有效改稿/模板，清除过时预览和确认；失败提示不得继续称新文件已加载。两项旧测试的“无效替换必须清空”断言已按新保护语义改写，原失败记录保留。脚本/非法字段仍拒绝。
原始草稿恢复前用同类DOM控件检查逐字可表示性，拒绝静默数字/换行转换。整批预检通过后才修改当前工作，不用例外补丁掩盖字段丢失。
原十二个模型能力档、质量名单、媒体/供应商/构建代码保持原字节；没有更新质量排名或新增商业API。

## 长期硬约束
Windows-first、local-first、单用户；文本真相、媒体不转码/缩小、候选只增不改。质量优先，无方案不自动降档。旧来源文件、项目工程、PROJECT_STATE/TASK及LAST_GREEN不覆盖。
不引入内部LLM/MCP/React/Electron/远端UI，不使用用户凭据，不自动上传、付款、生成、选型、选片或锁片。云端任务/计费状态不能由本地取消推断。主影片与离线作者台保持边界，无外部改稿自动写回故事/YAML/时间线。
原资源上限128MiB单媒体、512MiB去重总媒体、16导演锚点。收工缓冲元数据合计2MiB；原Studio最大512MiB+4MiB，外壳最多512MiB+8MiB用于有限元数据，不表示媒体上限提高。
收工包仅比旧总备份多保存上述暂存。主影片工程、外部NLE工程、未绑定文件、未应用能力档或组合预览、报告、播放器位置、临时撤回、账号仍不包含。没有后台自动保存或同步，也无加密/数字签名/自动脱敏。

## 背景事实与索引
源与安装版完成原始图视频收工恢复、未接受意见保留、原R15读内层及视频播放/解码；取消恢复和六秒成片演练亦完成。结构核验不是全视频解码或画质判断，具体层次读报告。
完整回归、原生file://双击、Windows生命周期及固定FFmpeg版本未标绿；Hypothesis/Ruff缺失应原样报告，不能重复假称全绿。商业生成调用为零。
- `TASK_R16_收工与接入边界_2026-09-12_08-00-50.md`：固定外壳、事务替换、识别和恢复约束。
- `REPORTS/continuation/R16_PROJECT_REVIEW.md`、`R16_INTERCHANGE_MATRIX.md`、`R16_VALIDATION.md`。
- `tools/delivery/QUICKSTART_ZH.md`：实际用户入口。
- `PROJECT_STATE_2026-09-12_04-45-30.md`：前期外部往返与完整进出决定。
- 原始失败与最终范围：包内EVIDENCE；交付恢复身份：PACKAGE、SHA256SUMS和独立receipt。

## 假设与未决
假设统一入口与收工包可减少用户记忆和漏存；没有长期真人使用或量化效率证明。原生Windows、任意NLE无损还原、整影片语义导回、大媒体分块、后台自动保存、多项目并行和真实商业执行仍待独立需求与验收，不以“用户模式”泛化授权。

## 变更日志
2026-09-12_08-00-50 UTC：记录R16统一接入/收工保存及防丢稿行为；旧快照保留。

2026-09-12_10-23-02 UTC：补充真实可下载交付、R17缺失边界、本轮原字节与恢复复验；应用仍为R16。
