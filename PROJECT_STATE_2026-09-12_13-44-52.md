# PROJECT_STATE

快照：2026-09-12_13-44-52 UTC。仅最新文件有效，旧快照保留。

## 目标与阶段
用户需要实际可用、灵活的外部工具往返，只用质量顶尖的模型，并反复明确要求文件能真正下载、解压和恢复。不能用聊天断言或版本号代替实物。
当前候选维护版 0.2.0+r16.3，阶段仍 R16。基于已验证完整 r16.2（Git 53b5329952b4ec0fa18356f83c3f6ba3dc9caa43，原包 SHA-256 e8639fc37d9fe21e75b01f1584f1a6ec0db6393da14d08f0272ef117f427a04e）。原 R17 未恢复，R3/R4仍是功能重建。最终文件是否成功发布及具体提交/哈希只读 PACKAGE.json 和当轮下载收据，不靠此状态预先断言。

## 有效决定与理由
新增跨工作区原素材重连，只修复缺失 File 绑定：内容哈希+字节数精确匹配，不按名字，不添加新候选、不覆盖已有绑定。预览可部分匹配，接回前再读取，工作或绑定改变就旧预览失效。已记录的视频和导演PNG复核实际元数据；原文、角色、镜头身份、审片历史及原字节保留，当前确认清除、质量优先。导出缺失清单只读且不算备份。复用旧格式，不新增导入协议。
续作请求超时现在包括正文，Promise.race 与 AbortController 约束全部读取；超时不是服务器未写入证明，必须重新查列表，迟到回执不得将旧稿标记成功。
保留可直接打开的 HTML 无 Python 入口和只读备用入口。可选标准库续作需用户主动开启，保存到独立 ManjuRecovery 目录，点为旧 Desk/v1 ZIP。每窗独立序列，新点flush/fsync、读回、原子发布成功后只清理本窗最近三份之外的旧点，失败不先删旧点腾空间。环回随机端口/路径/令牌、严格Host/Origin、固定路由；不公网发布。
本机恢复来源显式隔离独立备份标志，不把本机点当外部备份。完整影片工程、外部NLE与离线工作台分开。旧协议、旧能力档、旧工程和旧下载包不覆盖。

## 长期硬约束与偏好
只使用顶尖模型，不自动降档/付款/上传/生成/选片/锁片。原十二个能力档和质量名单不变；未重新排名。远程计费不由本地取消推断。LAST_GREEN不推进。
单素材128MiB、去重总量512MiB、16导演锚点限制不变；一次重连选择最多256文件。恢复点最大520MiB、默认目录2GiB/64份，每窗保留3份成功点，不清理别的窗口。自动续作空闲约8秒、相邻间隔至少30秒，不承诺卸载/关窗保存。
Desk包含三个工作区及已载入未接受的改稿/模板和相关文本，不含主影片目录、未绑定文件、未应用能力档/组合预览、临时撤回和播放器位置。没有加密/签名或云同步；标准库入口不是完整影片依赖安装器。下载须实际生成、解压、哈希和恢复核验后才出链接，用户电脑落盘仍需选回确认。

## 本轮实证与遗留
466项选定回归同进程完成：465通过、1跳过；23新增已包含，重跑不累加。四组源码/安装版真实媒体演练通过：重连找回5份素材、1920×1080原图保真、两视频完整解码；真实续作服务重启恢复。重连后的收工包源码/安装版同字节，原R16.2再次导出同字节。故障注入只删File绑定，不伪装为真实用户事故。
原生file导航被策略拒绝，浏览器为原HTML隔离文档，HTTP使用测试传输桥，未削弱生产源检查。初期CSP测试写法、错误预期、固定时间猜播放及工具时限中断日志保留。全项目完整回归、Ruff、Windows实机、固定FFmpeg、多浏览器/最大额定容量/断电矩阵未验收。不把上一轮6秒渲染当本轮实测。

## 任务索引
- TASK_素材找回_2026-09-12_13-44-52.md：机制、取舍、重现入口和限制。
- REPORTS/media-relink/VALIDATION.md 与 PROJECT_REVIEW.md。
- tools/workbench.relink.js / .html；tools/delivery/CONTINUE_CLIENT.js。
- tools/user_ready/rehearsal_relink.py；rehearsal_continue.py；build_continue_delivery.py（新产物命名MANJU_RELINK）。
- 原本机续作档案 TASK_本机续作_2026-09-12_12-30-33.md 和 REPORTS/local-continuation/ 保留。

## 假设
用户设备可能有Python3.11+，未验证。文件搬家/改名场景已用真实原素材加故障注入验证，不能推断可从损坏或缺失的磁盘恢复字节。大素材频繁完整快照有性能代价，尚无差分存储。

## 变更日志
2026-09-12_13-44-52：新增精确原素材找回和完整响应超时，保留旧原片、旧协议、质量名单及下载验证链。
