# 恢复点索引任务快照

版本0.2.0+r16.4，基于原r16.3，详细报告见REPORTS/recovery-index/。当前约束与目标见PROJECT_STATE_2026-09-12_14-37-01.md。

不复造旧协议或迁移记录。GET points/{id}/summary 需令牌/同源和预期SHA-256，调用RecoveryStore.load完整校验后解析。外/内ZIP元数据均有大小和条目上限，摘要不是恢复验证器；真恢复仍走ManjuDesk.preview。原DESK.zip及POINT.json没有新增字段。浏览器textContent展示用户文字，不拼进HTML；摘要短截仅用于显示。

保留顺序按sequence排序而非created_utc，避免时间倒拨误删较新点。部分失败不得删除原点。列表动态区域用hidden/aria-expanded，不依赖details pseudo内容；读摘要时触发的脏状态在退出busy后必须体现。不要改成强制点击、固定延时或把内容读取当成功保存。

复现入口：tests/test_recovery_index.py、tests/test_recovery_index_browser.py；实际视频使用tools/user_ready/rehearsal_recovery_index.py。下载用rehearsal_downloads.py测试最终真实五分段和实际保存。初版失败包括测试脚本current变量撞名、原生details可见性与旧状态提示；原日志保留。

后续未关门：Windows原生操作、最大额定容量、异常断电恢复与完整项目回归。不要声称摘要列出即视频健康，不要把沙箱浏览器下载当用户电脑落盘。商业生成为零，模型质量名单不变。
