# PROJECT_STATE

快照：2026-09-13_10-52-39 UTC。仅最新时间戳有效；旧快照不改、不删。

## 核心目标与实际阶段
用户确认实施上一轮五仓库研究，继续最高质量、本地创作、可外部往返与真实可下载交付。当前实现候选为0.2.0+r16.8，基于真实r16.7 Git `2c0b708e8e369c03ccd76755b3ca9b3f2149b0f1`、812提交；原包SHA-256 `ae74c0d1e1a1725f21e16455c4f2b5acd8a3ee7498016acd306a29bd9d87957f`。上一轮研究09:58状态已完整读过，本轮将其P0落地。最终HEAD/提交数/下载状态必须读取PACKAGE和最后审计，不在本快照提前填成功。
原R3/R4为功能重建，丢失的原R17及长篇尝试源码没有恢复；本轮不是对它们的字节级复原。

## 已生效的决定及理由
新功能位于既有镜头与素材视图，不另造新应用：可选故事工作本包含作品形态、稳定ID人物/关系/设定/事实/后果、真实顺序场景、引用职责、固定简报和人工复盘。单片/限定长篇/连载/短剧/音乐影像分流，不硬设镜长、集数、付费卡点或模型。
本场只选引用，默认资料仅作者可知。观众/指定人物/作者的范围显式声明；人物视角不继承其他场景正文。实际场序不按ID排序，前情限0–20场并显示省略数。范围检查不是自然语言防剧透，作者写入本场、自由文本、素材或ID中的信息不会被语义自动遮蔽。
简报固定当时引用内容与场序，动作同ID反转也会提示关联版本复核；未引用资料/未带入未来内容不造成无关失效。完全恢复原字节可重新显示一致，不是“从未修改”的历史认证。旧简报不自动重写、不自动重生成。
明确预览带入只替换当前镜头ID与提示词，清旧批准，质量优先开启，不改媒体/时长/画幅。已关联的简报过时、提示词被改或参考没绑定当前镜头时阻断规划。可明确解除关联，之后不再检查原故事联系。外发原参考按内容去重，一份可以多职责；职责不能证明供应商输入兼容。
人工实际画面/声音/偏差/返工层观察与原故事分离，不采用帧差阈值做自动创作裁决。
只读导入原series共享Bible，按文件种类+条目ID保持身份，保留出处与内容指纹；默认作者资料，不推断场景、不反写、不自动同步后来修改。已有series/core状态和媒体内核不重写。

## 数据兼容与保存边界
未建立故事时仍写Desk/v1；建立后写Desk/v2，明确包含故事、旧简报和复盘。3成员ZIP结构与原Studio/v1保持；旧r16.7只可取内层STUDIO.zip，不能读取新外层故事。页面明确提示原三工作区“总备份”不含故事，应该用顶部收工保存。旧包输入仍可读，不把协议变更藏在旧版本号。
故事JSON含整个作者工作副本，不含原媒体，外部导入为整本明确替换，不是逐字段三方合并。历史参考哈希不等于原文件；实际媒体仍由Studio现有绑定域保存，移除所有绑定后需旧备份/简报包与重新绑定才能再外发。临时预览、播放/滚动位置、主影片工程、外部剪辑工程和未绑定文件不在收工包内。
本机续作支持v2，摘要只读按故事标题查找，仍需主动开启。本机恢复点不等于第二份独立备份。

## 硬约束与确认偏好
只用顶尖质量，不自动降档、生成、上传、付款、选片、锁片。新Notebook/Brief作者协议不是执行provider。原12视频档、质量名单、静帧精修路线JSON逐字节不变，本轮未重新核验其当前市场能力或质量排行。LAST_GREEN不推进，未修改真实工程或旧归档。
不分发字体、第三方完整Skill/书籍语料或账号配置。旧五仓库研究只选择性借鉴，原创实现；两条X原帖上轮无正文，不补写证据。
普通HTML无需Python；可选续作Python3.11+标准库环回固定路由/令牌。媒体单文件128MiB、去重512MiB、16导演锚点；故事规范化JSON1MiB、512资料512场128简报，每场64引用32职责。不是无限库，大项目须另存分卷。恢复点按每窗序号保留3份，新点保存成功才清本窗旧点。附件不承诺永久下载，用户必须本地保存/选回校验并另做备份。

## 实证与未关闭事项
终态同进程937节点：936通过1跳过，含83新增故事相关用例；另18新增发布门测试通过。901份代码/测试/页面源文件指纹冻结。266运行时安装比对一致。
源/独立安装完整媒体演练完成，五原文件不变、1920×1080图保持。制作动作改变后旧版失效，建立新版本不覆盖旧版，明确链接后保存恢复、视频播放与旧r16.7内层回程完成。每次保存恢复ZIP字节一致；独立新简报ID随机，跨运行比较语义内容和媒体指纹，不谎称两份总ZIP字节一致。
首次窄屏min-content和hover对比问题修复；一次测试缺异步完成等待与一次脚本错误旧按钮ID记录保留。最终封包后还需全ZIP再安装及浏览器真实分段下载，这些只认最后下载审计。
全项目回归、Windows原生双击/安装/退出/升级/回滚、固定FFmpeg、最大容量/无障碍、商业API与真实画质未认证。成功浏览器为原HTML隔离文档。没有本轮整片重渲染。

## 档案索引
- TASK_故事制作闭环_2026-09-13_10-52-39.md：本轮对象关系、兼容与接管风险。
- REPORTS/story/README.md、PROJECT_REVIEW.md、VALIDATION.md。
- src/manju/authoring/story.py；tools/workbench.story.*；Desk/v2增量；4个story-* CLI与CONTRACTS登记。
- tests/test_story_sources.py、test_story_workbench.py、test_story_integrity.py、test_story_delivery.py。
- tools/user_ready/rehearsal_story.py、build_story_delivery.py；实际证据与最终审计随包。
- TASK_资料研究_2026-09-13_09-58-57.md和既有视觉/恢复任务继续有效，仅新变更覆盖相关范围。

## 假设
用户希望在既有离线台逐步管理故事，而非迁入大型新编辑器。本工作本是单用户制作桥接，不替代关系图数据库和多作者协作；尚未用用户唯一真实长篇材料验证体验。

## 变更日志
2026-09-13_10-52-39：实施研究P0，故事范围/实际场序、内容版本依赖、参考职责与实际复盘、Desk/v2保存和只读series接入；历史功能和质量路线保留。
2026-09-13_09-58-57：完成上传仓库/视频研究和反例；当时尚未并入应用。
