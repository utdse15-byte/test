# PROJECT_STATE

快照：2026-09-08T13:51:38.213566+00:00 UTC。仅最新时间戳有效，旧版保留。

## 核心目标与阶段
用户授权对真实项目全面梳理并直接落实个人使用改进。质量优先、只用顶尖外部模型和真实可下载交付的偏好不变。
当前阶段R13，基于实际R12 `d7a46588d23e3dda375d83016c1d02b2f9a8ac0a` 与完整历史。代码、同进程选定回归和独立安装演练完成；实际下载成立必须另由最终ZIP、PACKAGE和独立收据确认，不能从聊天或本状态推导已下载。
原R3/R4仍为功能重建，不是找回丢失原包。旧R12完整ZIP未改动，SHA-256为9edbc097c2362f44f12eb6aa749280f2f73ddaadf0b9c3f33635dc16c2977529。

## 有效决定与理由
个人首页按准备镜头、审片/定稿、局部返工、导演材料导航，不新增空模式开关。三个工作区可一起保存Studio/v1总备份，避免用户记住三个收工动作；旧Workspace/Repair/Director v1和旧批准哈希含义不变。
Studio保存原始半成品而不是强制转成可执行计划：空白、未完成目标、暂时非法返工范围、未提交意见、待定稿分辨率和返回像素均可保存。按哈希去重实际已绑定文件，不转码、不缩小目标图、不制造缺文件的空壳。
总备份只覆盖离线页面三个区，不包含完整影片工程、外部编辑器项目或未绑定文件。未应用能力档预览、派生报告和播放位置不保存；恢复是替换而非合并。
下载触发不是落盘证据。选回本地总备份只核验、不应用；仅当前视图一致才标为已核验，编辑后重新提示。独立全场指纹不受旧局部JSON导出清除dirty标记影响。
恢复先核验实物、再预览、再明确确认；异步文件操作或预览后变更拒绝覆盖。当前确认与型号选择清除，质量优先重新开启，历史审片不是新的批准。不执行、上传、付款、选片或锁片。
十二档目录与质量名单字节保持，R13未重新做X研究或质量排名；型号身份/日期限制沿用R12证据，不能将本輪UI改动当新模型实测。
STATE.md与FINAL_HANDOFF.md此前滞留R7，已修正并增加最新快照测试；README和两种包的说明统一以总备份为默认收工方式。旧快照、任务、LAST_GREEN原样保留。

## 长期硬约束与确认偏好
Windows-first、local-first、单用户；文本真相、媒体只增不改。用户外部创作质量优先，不为省费用或速度自动降档；代理执行仍严格零费用，不用用户凭据。
不引入内部LLM、新MCP服务、React/Electron或远端UI。派生报告不是生产输入。本地占位预演不是商业旗舰生成。
保留R2保存安全、R3取消与完成候选，以及人工批准边界。本地停止不意味着云端取消或停止计费。LAST_GREEN不推进。
旧PROJECT_STATE/TASK/ZIP不可改写。文件存在、哈希与独立恢复核验是交付前提；不能保证浏览器永久保存用户文件。

## 不可由源码独立推导的实测
632个不重复节点在单个进程完整运行：631通过、1跳过，65项R13新增全部通过。重复分组不累加。此前两次组合调用未产生终态文件，不作通过凭据；独立进程属主等到真实退出码0和XML后才认可终态。
源码和独立安装版最终8组真媒体演练通过：三工作区统一ZIP恢复、旧导演材料恢复、本地取消/恢复出片、真实截图取消。总备份包含5个去重媒体892744字节，1920×1080目标图原样，新窗口视频可直接播放。总备份ZIP、旧导演ZIP及六秒成片跨源码/安装版分别逐字节一致。
260个受测wheel运行时文件和源码/独立安装版逐项一致。原R12的18份状态/任务/绿灯及175项CLI保持，新增studio-verify，最终176命令条目。实际发布wheel与包数量由最终收据复验。
全仓仅完成6919节点收集，3模块缺Hypothesis，未执行完整回归；安装尝试未得到可用依赖版本，Ruff未执行。

## 索引
- REPORTS/continuation/R13_PROJECT_REVIEW.md：全局结构、真实缺口、已落实和不做的边界。
- REPORTS/continuation/R13_VALIDATION.md：终态、早期红项、实物演练及未关闭门。
- TASK_R13_总备份协议与验收边界_2026-09-08_13-51-38.md：协议恢复/下载语义及后续不能误判的事项。
- tools/delivery/QUICKSTART_ZH.md：首页总备份、只读回验、旧单区兼容和安装入口。
- PACKAGE.json/独立收据/EVIDENCE：发布包的身份与独立恢复结果；不属于模型输入。
- 上一状态PROJECT_STATE_2026-09-08_09-40-34.md，以及其中R12研究和任务索引保持有效的历史依据。

## 未决与假设
假设：一份总备份和清晰落盘状态能降低漏存概率；未有用户长期亲测的数据支持百分比收益。
Studio CLI验证闭包、字节和有界PNG结构，不重探视频或完整解码PNG。浏览器恢复另行复测视频/解码PNG，仍不是HDR母版或认证帧号，不保证商用模型遵循创作意图。
128 MiB单文件、512 MiB总媒体、16导演锚点和PNG32MiB/8192边长/33554432像素是本地上限。大于限制不会自动压缩以混过门槛。
Windows实机安装/原生file双击/退出/升级回滚、固定FFmpeg6.1.1、完整回归与真实商业模型执行/画质/费用未验收。当前Linux、Python3.13.5、Chromium144、FFmpeg7.1.5；isolated_document不能叫Windows双击通过。

## 变更日志
2026-09-08T13:51:38.213566+00:00：个人首页、三工作区总备份、下载回读、冲突保护、只读核验和最新接管入口完成选定测试与真媒体演练；以真实封包收据确认交付。
