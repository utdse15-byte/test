# PROJECT_STATE

快照：2026-09-12_10-59-13 UTC。只认时间戳最新一份；历史快照和任务均原样保留。

## 核心目标与当前阶段
用户以实际创作和可靠本地保存为先，要求直接实施高价值改进，不以版本号或聊天承诺代替实物。只用顶尖模型，频繁借助外部工具，必须有可下载、可恢复文件。
当前为真实 R16 的维护修订 0.2.0+r16.1，阶段仍为 R16。基线提交 ded624dda9344092d79dc5663cd3fa037145ec2a。R17 当前输入只有截图，没有可恢复源码或 ZIP；这次没有重建自动续作，也没有将本修订命名为 R17。R3/R4为功能重建，不是原失踪版本的字节复原。
最终交付身份、Git提交、归档哈希和恢复数量从 PACKAGE/本轮独立收据读取，不从聊天倒推。

## 有效决定与理由
修复 R16 的安装硬伤：pyproject、wheel、DELIVERY_VERSION 原为 r16，但 __version__ 是 r15；LAUNCH.install 版本门因此会拒绝并回滚其新建环境。真实旧wheel的独立安装确认了这一矛盾。新三处统一 r16.1，安装前的 VERIFY_PACKAGE 用 AST 只读解析版本并核对，防止未来复制旧常量，且不执行待验证代码。
只修改运行时版本，不改变作者工作台 HTML、十二个能力档、质量名单、媒体生成/取消/供应商/影片写入代码。版本验收不是新电脑的依赖安装验收。
新增可选只读本机入口 LOCAL_WORKBENCH.py：启动前将三个有明确哈希且有大小上限的固定文件读成内存快照；仅绑定127.0.0.1随机端口和随机路径，核对Host/Origin，不提供目录浏览、任意文件、上传或写入API。不依赖额外包；没有自动保存、恢复点、云同步、账户或远程UI。直接HTML入口保持，不强迫使用Python。
下载维护提供相同完整ZIP的五个普通小ZIP分段。离线助手先检查每份完整分段的大小和哈希，再取固定内容切片；验证每块内容以及总包后才提供另存，按内容身份排序，拒绝重复/缺失/篡改。不解析通用不可信ZIP，不用文件名当证据，不压缩或转码用户媒体。分段是传输格式，不是新的影片/收工协议。
START_HERE增加下载并选回测试文件的演练和分层排障。点击下载仅表示发起；完整性也不证明文件永久保存在用户电脑、附件长期在线、发布者签名或Windows认证。主影片仍需独立备份。

## 长期硬约束
Windows-first、local-first、单用户；文本真相、原媒体不缩小/不转码、候选只增不改。质量优先，不因无方案而自动降档。不自动选择型号、生成、付款、上传、选片或锁片。无内置LLM/MCP/React/Electron，无用户凭据使用。
旧项目和来源文件、旧ZIP、PROJECT_STATE/TASK、LAST_GREEN不覆盖。云端计费与任务状态不能从本地取消推导。主影片与离线作者台保持边界，外部改稿不自动写回故事/YAML/时间线。
原限制不变：128MiB单媒体、512MiB去重总媒体、16导演锚点；Desk缓冲元数据2MiB。新下载分段限制只针对本次发布包，不放宽创作媒体上限。
Studio/Desk/返工/导演等协议原样保持。Desk保存三个区与已载入的未接受改稿/模板/文字，未应用能力档、组合预览、播放器位置、临时撤回、外部NLE工程和未绑定文件仍不包含。没有自动保存、加密、数字签名或自动脱敏。

## 本轮实证和未决
终态选定回归160项通过，11项未选；原始失败和中断日志保留，不与局部结果合并成全绿。源码wheel真实安装到独立目录，r16.1运行时与distribution元数据一致；Python/第三方依赖来自已有测试环境，外网依赖下载因DNS失败，新电脑依赖获取没有完成。
浏览器在新建隔离文档中测试原始HTML：实际保存测试文件、选回核验、恢复旧收工示例、视频播放、再次下载并逐字节比对成功。本机HTTP协议的白名单/只读/Host/Origin等有真实服务测试；浏览器原生file和localhost导航被沙箱ERR_BLOCKED_BY_ADMINISTRATOR阻止，没有更改策略，不声称Windows或原生导航通过。
全项目回归、Windows安装/退出/升级回滚、固定FFmpeg版本、商用生成与费用/画质实测仍未标绿。没有更新模型质量排名。

## 档案索引
- `TASK_下载与启动维护_2026-09-12_10-59-13.md`：维护边界与关键取舍。
- `tools/user_ready/`：入口、分段源码、哈希实现、离线助手模板。
- `tools/delivery/LOCAL_WORKBENCH.py`：只读本机入口。
- `REPORTS/user-ready/VALIDATION.md`：本轮验证与真实限制。
- 上一轮完整状态：`PROJECT_STATE_2026-09-12_10-23-02.md`。
- 原R16背景/收工协议：`TASK_R16_收工与接入边界_2026-09-12_08-00-50.md`及REPORTS/continuation。

## 假设
假设小分段能帮助大文件下载不稳定的用户，假设本机只读入口能帮助部分受file协议限制的设备。用户设备、浏览器政策和实际网路不在本环境，不能保证这些假设对每台设备成立。

## 变更日志
2026-09-12_10-59-13 UTC：R16安装版本根因修复、封包前版本守卫、只读启动备用与下载分段；新文件另存，旧文件保留。
