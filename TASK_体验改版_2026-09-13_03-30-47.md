# 体验改版任务快照

2026-09-13_03-30-47，维护版0.2.0+r16.6。当前终态构建/文件交付以实际收据为准。

## 后续不能破坏的决定
呈现层不复制表单或改原协议；全部153个原具名控件仍存在一次。导航只是显示/焦点/滚动，不能改草稿、确认、文件和哈希。允许主动暂停隐藏媒体，不寻址、不自动恢复播放。导航/大字/长页偏好不能被当成待备份创作变更。
固定保存必须调用原Desk动作，不代替验证；Ctrl/Cmd+K只查功能，不能自动生成或批准。异步原导入需要显露正确视图，可选本机续作面板需要动态注册home。
保留完整长页模式和原区号解释。旧原生多区功能测试应明确进入用户可见showAll，默认分区必须另有测试，不用force点击跨隐藏或禁用状态。

## 实证入口
EVIDENCE/regression.xml 765通过1跳过；source-experience/installed-experience真实媒体含Ctrl+S、选择文件、播放、旧版往返；source-retouch/installed-retouch兼容演练。installed-parity核265个运行时文件。测试过程首轮Esc与旧夹具/版本标签失败、中断rc-15保留，最后整组完成才有效。
源码与安装收工ZIP相同，hash 9a3fd5bb89007a320f0686643aade3d807b3696c9fb49d0b4d6886a8b11bce6d。原r16.5未改HTML能再导出同字节。

## 尚需真实环境验证
Windows原生双击/续作导航/安装/退出/回滚、全项目回归、容量极限、系统字体差异；本轮隔离Chromium不是Windows认证。最终下载包的wheel再安装与实际5分段合成由封包后收据记录。不要把构建单元测试或源码演练代替最终包验收。

## 继续开发入口
REPORTS/experience，tools/workbench.experience.*，tools/user_ready/rehearsal_experience.py / build_experience_delivery.py。旧工作流核心和模型目录本轮没有改变；问题优先修呈现层，不为UI兼容改数据哈希。没有跨页面UI偏好持久化，后续若增加需要独立轻量设置且不能悄悄修改旧Desk协议。
