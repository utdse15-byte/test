# UX Wave 2 — owner-experience improvements (2026-07-17)

Branch: `claude/ci-validation-improvements-rsmjpf` (stacked on the CI/validation
wave). Source: the owner's ranked 13-item experience/functional/engineering
audit. DECISIONS entry: UX-WAVE-2. Tests: `tests/test_ux_wave2.py` (21, all
behavioral per tests/CONVENTIONS.md).

## Landed

| # | Item | Where |
|---|------|-------|
| 1 | Completion notifications: Web Notification (permission on first job click), `(N 完成)` title badge, opt-in sound; hidden tab + active job keeps a slow 20 s check instead of a full stop | `gui/page.py`, `docs/GUI.md` |
| 2 | `gen:S00X (n/m)` within-phase progress on the existing `on_phase` channel (advisory-only emitter, never a cancel checkpoint) | `build/graph.py` |
| 3 | did-you-mean on mistyped commands (`manju bulid` → 你是想输入 manju build 吗) — difflib, same technique as `core/mentions.py` | `cli.py` `_SuggestingGroup` |
| 4 | Shell completion available as explicit opt-in (`manju --install-completion`); frozen leaf surface untouched (snapshot suite green, no regen) | `cli.py` |
| 6 | Show in Folder: `POST /api/reveal` (inside-project only, readonly-exempt), 📂 button next to the latest final; `explorer /select,` on Windows | `gui/server.py`, `gui/page.py` |
| 7 | `manju voice S00X --preview --voices a,b,c` — one cached preview per candidate voice for side-by-side listening | `cli.py`, existing `media/ttspreview.py` cache |
| 8 | `manju new --demo` — 雨夜便利店 12-shot time-loop, text truth only, all shots pinned to free `caption_card`; dry-run cost == 0 is a test | `presets/demo.py`, `cli.py` |
| 13 | `AI_IDE_*` era (46 closed reports) → `REPORTS/archive/` | `REPORTS/` |

## Deferred (recorded, not silently dropped)

* **Item 2 (second half)** — ffmpeg `time=` percent within one render segment:
  needs a progress channel through `media/render`'s logging; follow-up.
* **Item 5** — Ctrl-K command palette: wants the static-JS extraction first.
* **Item 9** — mobile review package / `--lan`: changes the localhost-first
  threat model → its own DECISIONS process before any code.
* **Item 10** — MCP export-tool coverage (EXPERIENCE-AUDIT A8): own wave.
* **Items 11/12** — `cli.py`/`page.py`/`server.py` splits and `app.js`/`app.css`
  extraction: still gated on both platform gates green on one commit
  (standing rule: refactor only after behavior is covered).

## Verification

* `tests/test_ux_wave2.py`: 21 behavioral tests (CLI suggestions, completion
  options, n/m progress via a fake provider, reveal endpoint incl. path-escape
  refusal, per-platform argv, voice comparison loop + JSON envelope stability,
  demo structure + `run_check` + zero-cost plan + flag exclusivity).
* Frozen surfaces: `cli_surface.json` snapshot suite green (new options are
  optional/leaf-invisible); no existing test weakened.
