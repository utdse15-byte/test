# r16.4 验证范围与限制

日期：2026-09-12。版本：0.2.0+r16.4。依据本轮实际源码、独立安装版、文件和原始日志，不代表 Windows 正式发布或全部项目回归。

## 基线和实际修复
完整原包 MANJU_RELINK_FULL.zip 的 SHA-256 为 e79ef20b4483e7e6f8fea9c9499a3e6fcab743faade2397d626e1c7a0d9e8524，Git 为 55990e7408e22679d265b4693ad13e4f5eb89d2a。先读取完整最新状态并恢复其 Git 历史，原 ZIP 没有修改。

本轮在可选本机续作中增加只读内容摘要和筛选：读盘校验完整 ZIP 哈希，读取有上限的小型文字元数据，不提取或解码媒体、不修改原记录、不恢复或批准。重新读取不依赖旧缓存；坏点保留，其余点继续。长文字仅显示摘录，筛选不是全文搜索。列表可显式展开/收起，读摘要期间的新输入仍显示未保存。

修复本窗保留点按墙钟排序的错误。真实合成收工包复现：连续保存序号1到5，时钟在第3次倒拨，原版保留1、2、5，修复后保留3、4、5。依据保存序号，只在新点发布成功后清理本窗旧点；不触及其他窗口。见 clock-rollback-reproduction.json。

## 终态选定回归
final-regression.xml：509 个不重复测试点，508 通过、1 跳过、0失败、0错误。14个模块同进程完成。新模块共43项，已包含在总数中，重复复验不累加。范围在 selected-tests.json。

新增覆盖只读摘要、旧格式、完整哈希、非法字段、元数据上限、私有HTTP权限、路径拒绝、长文保留、时间倒拨、坏点继续、旧缓存重新核验、停止后续读取、XSS按文本展示、新输入保护、宽屏重启列表与键盘收起/展开。唯一跳过仍是原 file:// reload 测试。

终态另有封包/下载传输/入口复验，与以上存在重叠，具体值见独立 XML 和最终收据。不能将其加总成完整覆盖率。

## 六组实际媒体演练
源码版和独立安装版分别运行原续作、原素材找回、新摘要检索三组演练。新演练存两份不同镜头，在第三份未保存新稿上只读检索；恢复目录所有文件和当前草稿不变。停止服务后重新启动，按镜头查到正确点，原有核验预览/确认恢复后播放实际视频，再另存原格式收工 ZIP。

五份媒体的原字节保持，1920×1080目标PNG未缩小；两个视频经FFmpeg完整解码。原 r16.3 页面能打开新恢复点导出的Desk/v1，并重新导出逐字节相同的ZIP。源码版和安装版索引演练导出哈希一致。尚未接受的外部意见与模板仍待处理，不转成批准。本轮没有重新渲染六秒影片，不拿旧轮结果当本轮实测。

## 首次失败与纠正
first-browser.xml 中一项失败暴露重复读取开始前残留旧“完成”状态，已改为读取前同步显示开始状态。dirty-status-before.xml 先复现读取期间输入已被观察后仍沿用旧保存提示，随后补上读完后的未保存提示。

源媒体演练初版变量名与服务连接上下文冲突，首份日志保留；修正变量后，在真实重启流程中又发现原生details虽有open属性但内容仍不可见。restart-debug.png 与诊断记录显示 ::details-content 保持 hidden。没有用强制点击或固定延时绕过，而是将这一块换成普通区域加显式显示/收起并补宽屏重启测试。源码和安装版再次完成实际点击。旧失败记录在 history/，不删除红项。

## 最终交付
封包使用干净已提交源码，受测文件哈希门、六组演练结果、独立安装版本身份门。完整源码、wheel、Git bundle及从R2的补丁同包。最终ZIP新目录解压后重新核对清单、运行时文件、历史恢复及补丁重放，完成才复制为排他的新下载文件。精确提交、文件数、大小、SHA-256读实际PACKAGE.json与下载收据，不预填未生成哈希。

下载助手和五个分段会用最终真实大包重新演练乱序合成、实际浏览器保存及选回核验，最终收据区分这些验收和运行时测试。下载演练是沙箱浏览器，不证明用户电脑已经收到。校验码证明字节一致，不证明发布者身份。

## 尚未验收
未跑全项目回归或本轮全量收集，未运行Ruff；不沿用旧节点数。Windows实机安装、双击、退出、回滚及固定FFmpeg版本未验收。native-navigation.json 记录本轮原生 file:// 导航确实被沙箱 ERR_BLOCKED_BY_ADMINISTRATOR 拒绝，页面测试是原始HTML的隔离文档与真实本地HTTP传输桥；生产Host、Origin和令牌保护未放宽。

未跑最大额定素材、2GiB/64份完整容量、多浏览器、突然断电或磁盘故障矩阵。每份摘要需要完整文件哈希，停止请求仅阻止后续读取，当前读完或超时后结束。续作需要已有Python3.11+，普通离线入口不需要；完整影片应用依赖另行需要。十二个能力档、质量名单及媒体核心保持原字节，没有商业生成、付费、云同步或真实用户影片改写。LAST_GREEN不推进。
