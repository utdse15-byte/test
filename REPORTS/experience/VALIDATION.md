# R16.6 体验维护验证记录

## 基线与范围
基线为实际 MANJU_CURRENT_FULL.zip，SHA-256 `5a53f6bd6d29206266690422265d4018ff60297579c6cdd82f601b745e2048bc`；Git `4f0b3b273101c7e960fcbf63efb2d43351d22ffc`，810个提交，运行版本0.2.0+r16.5。本轮版本0.2.0+r16.6。原包和历史快照不覆盖；最终HEAD、完整文件数量、归档大小与SHA仅以实际封包收据为准。

本轮改的是离线创作工作台的呈现与入口，不是主影片时间线或生成核心。没有商业模型调用或重新排名，没有重跑六秒整片渲染，不能借用前轮结果冒充本轮。

## 已完成选定测试
`regression.xml/.log/.rc`记录同一进程完整结束：766个节点，765通过、1跳过，退出码0。包括45项新的默认分区体验测试（43项UI、2项真实本机服务），以及原有收工、素材、导演、返工、质量名单、组合、外部往返、精修、恢复摘要与交付测试。45项包括不同视图与360/390/768/1440宽度、可见性、焦点、快捷键、IME、滚动、错误提示、无远程请求、完整视图切换、真实下载与备份状态。

`delivery-and-review.xml`另记录79项通过，含9项新封包证据门测试。这个范围与766重叠，不能相加。文档收口后入口复验记录另保存，不据此扩大覆盖率。

唯一跳过为测试环境不允许原生file://重载的既有用例。没有执行全项目约7000项回归，也没有声称本轮完成全量收集。Windows原生双击、安装、退出、升级回滚、固定FFmpeg、最大额定容量和完整无障碍认证均未做。

## 实际图视频演练
`source-experience/RESULT.json`与`installed-experience/RESULT.json`：使用完整原HTML的Chromium隔离文档，实际文件选择器载入收工包、明确恢复、播放视频、切换暂停并保持时间、查找入口、大字/完整视图切换、修改文字、在导演视图用Ctrl+S实际下载、选回核验。全部执行，无远程请求或JS错误。

五份实际媒体哈希保持不变，1920×1080目标PNG没有缩小。新页面再次恢复/导出，与原封不动r16.5页面恢复/播放/再导出，两者均与本次保存出的ZIP逐字节一致。源码与安装版收工ZIP同为 `9a3fd5bb89007a320f0686643aade3d807b3696c9fb49d0b4d6886a8b11bce6d`。

`source-retouch/RESULT.json`与`installed-retouch/RESULT.json`重新运行既有精修回程：2秒640×360合成视频、两张1920×1080目标PNG、分批接回、冲突拒绝、其他工作区保护。两版的精修ZIP、导演ZIP、收工ZIP均逐字节一致。这是兼容回归，不是本轮新增商业画质成果。

`installed-parity.json`核对265个运行时文件与源码一致。构建时会再次生成真正的交付wheel并核验其文件。最终外层ZIP解压后安装演练与实际分段下载另由最终发布审计收据记录，不预写成功。

## 原始失败与修正
首次体验测试两项失败：搜索输入原生Esc行为先消耗按键，已加明确退出；测试直接访问折叠说明中的字段，改为实际展开原入口。原日志保留。

首次扩大回归因旧测试硬编码R16阶段标签、另一个旧审片模块重复夹具未切换到可见完整模式而失败，随后测试进程中断，原部分XML和rc-15保留为regression-attempt1。修正为实际维护版本与用户可见完整模式后，才有终态766完整记录；没有将部分结果覆写成全绿。

旧数据测试通过用户可见完整长页模式操作，新的默认分区和真实媒体演练单独覆盖，未使用强制点击跨过隐藏/禁用检查。精修演练旧证据字段误写固定r16.4，现改成通用legacy_director_roundtrip_identical并记录实际旧HTML哈希；修改前后记录均保留。

## 封包与下载门
封包器要求完成的pytest XML/rc、受测文件指纹、源码与安装精修证据、默认分区实际下载/旧版兼容证据。缺失或不同即拒绝发布。完整包要求安全路径、无字体、ZIP CRC、清单哈希、Git bundle恢复、祖先和R2补丁重放、wheel源码一致；上手包页面与完整包相同。五个分段必须还原同一个完整ZIP。

下载助手实际浏览器演练另外核验完整包、中文改名上手包、截断拒绝、缺失/重复分段拒绝、乱序合成、浏览器保存并选回，以及入口页的保存测试。最终结果在MANJU_EXPERIENCE_DOWNLOAD_AUDIT.json，不能用封包器单元测试代替真实大文件操作。

这里的浏览器是隔离文档，不是原生Windows导航，也不是用户电脑已经保存。选择自己的实际文件核验仍是最后一步。附件不提供永久存储保证。

## 文档收口复验
最终入口与封包门复验42项通过（final-entry.xml），与前述范围有重叠。此前未设置隔离传输参数的一次原生file://尝试，40通过、2项页面建立报错，原记录保留为final-entry-native-blocked；随后仅按已声明的隔离传输方式运行，没有修改产品或绕过原生策略，不能将其标成原生导航通过。
