# R16 用户就绪维护：验证记录

## 修复与交付边界
阶段仍为R16，维护版本0.2.0+r16.1。修正 __version__ 与pyproject/wheel/DELIVERY_VERSION冲突；原值会令LAUNCH.install拒绝已安装的R16并回滚该次新建环境。新增AST只读版本守卫，防止到用户安装时才发现。原作者台HTML、十二个能力档、质量名单和媒体核心不变。

新增只读loopback备用入口及启动页面保存演练；完整包下载可走五个普通小ZIP分段，离线助手检查每块、总包并提供另存，命名顺序不作为身份。它们不增加自动保存、云同步、付费生成或R17恢复能力。

## 实际结束的测试
终态选定回归160通过、11未选，含84项安装/包/本机HTTP检查与18项下载分段/浏览器保存检查，范围有重叠不可叠加计算。原始记录见本轮证据。
真实旧wheel在独立目录安装后，distribution为r16但运行时为r15；修订wheel真实安装后两者均r16.1。安装使用pip --target --no-deps和已有第三方依赖；不是独立新机依赖解析成功。
浏览器新建隔离文档实际运行原始入口/工作台：保存测试文件、选回核验、附带收工ZIP恢复、四个视频控件播放、重新保存收工ZIP及选回验证通过；新ZIP与示例逐字节相同。390像素页面无横向溢出、无脚本错误。
本机HTTP检查为真实socket服务：只允许指定文件、Host/Origin限制、令牌路径、拒绝POST/目录/路径变形、启动后文件变化不影响已核验内存快照；未完成实际Windows浏览器导航。

## 原始失败与限制
先写版本一致性回归，旧源码失败，实际安装版本也复现矛盾。扩大组合运行在200秒工具时限中断，无完整终态，不能据点号计数。一次定向选择误含浏览器测试且没有设置隔离传输，37通过1错误；之后终态160范围完整记录。
浏览器原生file://和localhost导航被ERR_BLOCKED_BY_ADMINISTRATOR阻止；没有绕过策略。第一次演练复用失败导航上下文和多文档set_content，未完成；最终用每文档独立新页面重写演练并通过。此方法不等价于Windows双击或原生localhost端到端。
外部依赖下载DNS失败，未自动反复尝试；已有环境完成wheel安装和导入核验。全项目回归、Windows安装/升级/回滚/退出、固定FFmpeg版本未验收。没有商用模型调用或新质量榜单结论。

## 最终文件验证
最终完整包独立解压、源码/wheel一致、Git恢复/祖先关系/补丁重放，以及真实大小的分段重组结果，记录在包外MANJU_READY_VERIFIED.json。源证据与回归记录保存在源码快照的报告和交付EVIDENCE_READY中。最终收据不会由本报告代填成功。

## 官方依据与采用范围
- Python 3.11 http.server 官方文档警示它不是生产服务器；本机备用不是默认目录服务器，使用自定义精确路由且仅监听127.0.0.1。https://docs.python.org/3.11/library/http.server.html
- MDN说明 download 属性不能判定下载是否发生；页面持续区分发起下载和用户选回真实文件核验。https://developer.mozilla.org/en-US/docs/Web/API/HTMLAnchorElement/download
文档依据用于设计边界，不替代本地实际测试。
