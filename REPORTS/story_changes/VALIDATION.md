# r16.10 · 可读故事差异的验证记录

## 身份与范围

基线为实际下载的 `MANJU_IDE_FULL.zip`，Git `4537740`，版本 `0.2.0+r16.9`。候选版本 `0.2.0+r16.10`。原包和真实用户工程均不修改。新增只读故事对照、既有导入/恢复预览、IDE 的可选 HTML 报告；不增加逐项接受、三方故事合并、商业生成或常驻 AI 服务。

## 完成的终态回归

同一个 pytest 进程执行 **1,084 个不重复测试点：1,083 通过、1 跳过、0 失败**，实际退出码 0，224.38 秒，4 条原有 warnings 保留。包含 **71 项新增测试**，全部通过。原 r16.9 的 1,013 个选定节点保留，新增三个模块。此范围不是全项目回归。最终接管/上手入口另复验 4 项，全部通过，不并入上述计数。

记录在 EVIDENCE/regression.xml、regression.log、regression.rc、regression-summary.json。第一次扩大运行的 1,077 点为 1,075 通过、1 失败、1 跳过；失败发现 START_HERE 仍写旧版本，随后修正入口并重跑整个选定范围，没有放宽断言。独立封包检查 7 项也包含在上述 71 项，不重复累计。

## 新增检查的重点

Python 与浏览器在 19 类快照情景逐项比对：动作、重复台词、改名、重排、插入、删除、引用范围、结局、新复盘、新简报、依据排列、新增依据、Unicode/空白、HTML 文本、不同项目、空/新增/移除/未改。按稳定编号识别，同名不代替同身份；插入导致的后移不把旧正文冒称重写。

原文与新文完整保留，不因显示而截断数据；页面使用文本节点，离线报告进行 HTML 转义且不含脚本或操作控件。报告排他新建，不覆盖旧文件。非法模型和被不正确修改的实例仍重新校验。

网页预览是当前页面对待恢复文件，不是导出原始基线；继续编辑会清除旧对照和确认，取消不改变作品。坏新 JSON 清理旧预览而保留当前正文。360/390/768/1440 像素检查没有横向溢出，窄屏采用上下对照。信息性预览不重复弹 Toast 遮挡正文，实际错误仍显示。

## 实际文件与浏览器操作

源码和独立安装 wheel 均运行 tools/user_ready/rehearsal_story_diff.py。由浏览器实际保存旧示例收工 ZIP，再由子进程执行 ide-open、ide-preview --html、ide-return。脚本只改第二场动作与对白，模拟 IDE 文件编辑，不冒称调用了商业 IDE 的 AI。

浏览器同时继续写新稿后，打开返回包能看到这份新稿与候选的对照；预览和取消保持当前指纹不变。过期预览被拒绝。之后在独立受控基线上明确恢复候选，播放实际视频，保存、选回核验、新窗口重开并重新导出。原封不动 r16.9 页面能够恢复新包并逐字节重导出。

源码版与独立安装版的只读 HTML 报告、最终收工 ZIP 分别逐字节一致。五份素材逐字节保留；另独立读取实际 PNG IHDR，确认 1920×1080 目标图保持原文件。268 个安装运行时文件逐项匹配源码。模型目录、质量名单和图像路线等五份原数据文件全部逐字节不变。

源码/安装证据分别在 source-ide-complete/、installed-ide/；安装路径不同，不能拿源码目录代替独立安装。actual-media-parity.json、installed-parity.json、preserved-model-data.json 为独立核验记录。最后发布包重新安装、分段浏览器合成与实际下载回读，只在完成后记录到最终 DOWNLOAD_AUDIT，不由本报告提前宣称。

## 保留的早期失败与工具边界

先执行原 21 项 IDE 基线，全过；新增“动作变化应含原文新文”的测试在原逻辑上失败，随后修复。初次浏览器测试未导航到故事区、另一次尝试引用闭包内部函数，都是测具问题；改为真实导航/用户动作，没有为了测试开放产品内部接口。两个工具调用超时的运行不计为通过。独立 wheel 首次使用错误 cwd 未找到 pyproject，明确失败后改为绝对源码路径，原失败日志保留。最终入口初次复验还发现接管文档漏写累计阶段名称，并有两项因漏设既有隔离文档选项触发原生导航阻止；文档补回 Manju R16 标识，使用原有隔离传输后四项通过。没有改动浏览器策略或测试断言。所有历史阶段日志与终态分开。

## 交付门和未完成范围

交付门必须同时取得终态 XML 和退出码、受测源指纹、独立安装与源码两条真实文件回程、报告/页面/媒体一致性。发布在清洁提交之后；实际 ZIP 独立解压、清单、wheel、Git 恢复、祖先与 R2 补丁重放分别核验。文件核验不等于用户电脑已经落盘。

没有全项目回归、Windows 实机双击/完整依赖安装/退出升级回滚、固定 FFmpeg、最大容量、完整辅助技术认证。沿用原 native file 重载跳过；浏览器运行完整原始 HTML 的隔离文档，不冒称原生 Windows。没有商业模型、商业 IDE 客户端或整片渲染。LAST_GREEN 不推进。

只读对照不自动接受或合并，恢复仍然整体替换。结局字段相同不是故事语义或因果不变；差异不证明实际作者或模型身份。报告可能包含作者计划和旧版本，分享前需要查看范围。
