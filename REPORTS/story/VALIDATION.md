# r16.8 故事制作桥接验证

## 实际基线
MANJU_FLOW_FULL.zip，r16.7 Git `2c0b708e8e369c03ccd76755b3ca9b3f2149b0f1`，812提交。原归档不变。目标运行时0.2.0+r16.8；最终Git提交、数量与下载哈希以封包收据为准，不用聊天命名替代。

## 终态选定测试
同一进程937个不重复测试点：936通过、1跳过，退出码0。主证据 regression.xml/.log/.rc。包含83项新的故事数据/浏览器/完整性测试，原r16.7选定范围以及额外原系列与CLI兼容检查。不是全项目回归。
另有18项新发布门测试通过（story-delivery.xml/.rc），不把其他重复运行加到覆盖率。最终接管入口复验另记。

覆盖：同ID动作反转、未引用资料不误标、重排实际场序、有限前情与省略数量、作者/观众/指定人物范围、未知图关系拒绝、版本哈希、人工观察不改正典、原参考去重/字节、闭合集ZIP的额外/缺失/篡改/危险路径拒绝、旧Desk/v1与新Desk/v2、旧内层Studio、明确带入及异步预览冲突、XSS文本、窄屏360/390/768/1440、焦点与hover可读性、原恢复点读写/摘要、现有系列只读接入、CLI拒绝覆盖。

## 实际文件与浏览器
source-story/RESULT.json 与 installed-story/RESULT.json 使用完整未改写HTML、真实合成媒体与实际用户控件，完成：旧收工包恢复 → JSON预览导入故事 → 职责显示 → 实际下载简报和原视频 → 明确带入镜头 → 同ID动作反转使旧版待复核 → 建立新版本且不改旧版本 → 记录人工观察 → 快捷键下载新收工包 → 选回核验 → 新窗口恢复 → 播放视频 → 再导出。

每次保存/恢复再导出的ZIP逐字节一致。5份原素材保持字节，1920×1080目标PNG保留；一份视频承担两种职责的简报外发只含一个实际原视频。两种运行方式的媒体哈希与两版简报内容哈希一致。新建简报ID具有随机性，独立运行的整个收工ZIP不强行要求相同，未伪称跨运行原包逐字节一致。

独立安装检查匹配266个运行时文件，使用安装目录的包资源而非源码影子。最初出现的UI长列溢出已改成minmax(0,1fr)正确收缩，不用隐藏页面裁切；活跃标签悬停文字对比问题也经实际查看修复。

旧r16.7页面真实打开新包内层STUDIO.zip，能播放并再次导出相同字节。它不能打开新Desk/v2外层，未将内层兼容偷换成新故事在旧版完整恢复。

## 原始失败记录
story-browser-first.xml记录18通过4失败：三项真实窄屏min-content布局问题和一项测试未等待读取完成就检查异步保存状态。产品布局被修复，测试改为等待实际DOM核验回执，不用固定延时猜成功。story-browser-final后续22通过保留。
第一次真实旧版回程脚本误用了不存在的按钮ID restore-studio；日志完整保留。核对原HTML后使用既有apply-studio，未修改旧页、强制点击或绕开核验。之后两次完整素材演练均完成。
最终接管入口首次检查还暴露了新中文说明缺少原稳定阶段标识，以及该次运行未声明隔离文档模式而被沙箱阻止file://导航。随后统一入口版本与保存边界措辞，按原隔离文档测试方式复验，未修改产品CSP或测试验收规则；原失败日志保留。

更早所有targeted日志均保留。若终态交付门失败，不得用这份报告覆盖失败或发布。

## 封包与下载门
新build_story_delivery.py使用自己的证据门，不伪造此前无关retouch/inspection标志。要求终态回归、当前源文件哈希、源/安装故事实素材结果、266运行时一致；然后使用原全量源码/wheel/Git bundle/R2补丁恢复验证器。旧包与同名目标拒绝覆盖，字体与敏感文件不新增分发。
最终发布后还需在新目录解压、安装包内wheel、再跑完整故事演练；配套五个真实分段需在浏览器乱序选入、合成、下载并选回比对。最终结果在外层 MANJU_STORY_DOWNLOAD_AUDIT.json 中记录，这份打包前文档不预写它们成功。

## 没有认证的范围
Windows原生file://双击、完整依赖安装、退出/升级/回滚、固定FFmpeg版本、全项目回归、最大容量压力、完整无障碍、真实商业模型画质与费用均未验收。原生导航在此沙箱受限；浏览器运行原HTML隔离文档，不冒称Windows原生端到端。此轮没有商业生成或整片重新渲染。哈希是内容一致性，不是数字签名、语义/剧透判定或来源真实性证明。附件不是永久备份，用户电脑落盘仍需选回核验。
