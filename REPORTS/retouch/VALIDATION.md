# Manju 0.2.0+r16.5 验证报告

基线：r16.4，Git 1f462282578500c641cf5101d11ab730ea1e15f2；原ZIP SHA-256 fddf7e0f508da75b4c176c655bbbb848238d08896373419ad1ca4b0559cc0e6d。研究截止用户当地2026-09-12，执行记录采用UTC时间。最终交付成功、版本、提交和哈希以实际恢复收据为准。

## 已完成的选定回归
`evidence/regression.xml/.log/.rc`：同一个进程713个测试点，712通过、1跳过、0失败、0错误，退出码0。39项新增精修测试包含其中。56项局部测试和后续封包/入口复验与它有重叠，不累加为覆盖率。不是全项目回归。

另有41项交付门、安装版本身份、只读入口与接管文件检查实际执行通过（delivery-tests.xml/rc），其中8项新增封包拒绝测试；不与713项合并成完整项目覆盖率。

覆盖逐图任务、路线快照、篡改和文件名、共用与逐锚点上下文、分批回传、重复幂等、过期本地图拒绝、画幅和PNG解码、批次原子性、原素材/其他区域保护、家族质量名单、CLI协议、旧导演/Studio/Desk进出、重连和本机恢复摘要/保留规则等。

## 真实文件演练
`source-retouch` 与 `installed-retouch`：实际FFmpeg生成2秒640×360运动视频，真实浏览器播放并抓取两帧；导出ZIP和逐张TXT/PNG，返回两张1920×1080合成PNG。分别接回、重复导入不改、其他工作区不动。保存导演ZIP和收工ZIP，Python独立核验原片与图像字节。

源代码和独立安装wheel的265个运行时文件逐项一致，两版精修ZIP、返回后导演ZIP与收工ZIP分别逐字节一致。原封不动r16.4页面恢复新导演ZIP、播放视频并重新导出，字节一致。新功能没有改变旧导演/Studio/Desk协议，也没有修改12个视频能力档或质量名单。

所有返回图为明确的合成测试，不是商业AI生成，不证明画质或供应商稳定性。本轮没有商用调用、费用/速度比较、自动剪辑或整片生成。没有重跑旧轮六秒渲染，不引用旧轮数据冒充新测试。

## 原始失败不覆盖
首次局部运行有8项失败，其中7项是测试脚本用字符串表达式触发生产CSP，另1项错误地把相同图片的重复回传当作应拒绝。修复测试脚本为函数表达式及真正不同的测试图，不放宽生产CSP或幂等保护。第二次有1项测试未准备必要的主镜头文字，修正测试准备后通过。记录均保留。

原生file://导航实测报ERR_BLOCKED_BY_ADMINISTRATOR。成功浏览器演练运行完整原始HTML的隔离文档，不修改页面源码、不跳过核验。唯一跳过项与原生file://重载有关。不能替代Windows双击、安装、退出、升级回滚或跨浏览器验收。

## 封包门与界限
新封包器要求真实回归退出码和XML、源码/安装版演练结果及相同哈希，运行时受测文件有指纹保护。完整包由既有独立恢复器新建，验证ZIP CRC、路径、源文件哈希、wheel一致性、Git历史与R2补丁重放。不覆盖旧包。五个传输分段与内容绑定助手用实际最终包测试；最终下载行为与第三次安装演练的具体结果另见下载审计和外附证据，以实际成功记录为准，不在源码报告预先宣称成功。

完整影片应用依赖仍需安装；Hypothesis/Ruff本环境未提供，未执行全量回归或Ruff。模型入口只作离线说明，未签名的哈希用于误改/损坏检测，不构成发布者身份认证。附件非永久托管，用户本地保存必须选回核验。LAST_GREEN不推进。
