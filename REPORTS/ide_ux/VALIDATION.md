# r16.9 · 验证记录

## 身份与范围

基线r16.8 / Git 7f7d4d7848b0a574356dc182d648805e1da51d0f；候选0.2.0+r16.9。原上传包不改，真实用户工程不改。最终HEAD和归档大小以PACKAGE与外层核验收据为准。这里没有提前宣称用户电脑已经收到文件。

## 选定终态回归

同一个pytest进程执行 **1013 个不重复节点：1012通过、1跳过、0失败**，实际退出码0。包括 **58项新增**（IDE工作目录21、冷接管5、UI11、封包门21），全部通过。原r16.8终态选定937节点完整保留，额外纳入18项既有故事封包检查，不把这18项冒充新开发。

最终原始记录在EVIDENCE/regression.xml、regression.log、regression.rc、regression-summary.json。第一轮992点991通过1跳过；此前121点专项、26点新入口/封包检查都有重叠，不累加成覆盖率。原native file重载跳过保留，不改为无条件通过。4条warning原样保留。

冻结927份运行时、测试和页面源文件，封包前逐项重核验；原210个具名输入控件逐个保留且ID唯一，新增4个视图级操作。没有通过删掉字段或放宽原校验来换绿灯。版本、CLI面195子命令和2新作者协议已登记。

## 三条真实工作链

1. 源码与独立wheel安装：用实际故事收工ZIP恢复五份图视频，写动作，切三步骤与故事/镜头视图，浏览器真正下载给AI的收工包；外部终端真实执行ide-open/preview/return，脚本改动作/人物依据/镜头提示，原基线和五媒体不变；旧简报保留并待复核；新窗口恢复并播放原片，1920×1080目标图原尺寸可见，再保存、重开、再导出字节一致。
2. 旧版兼容：原封不动r16.8页面打开新版的Desk/v2外层，能播放，重新导出的ZIP与输入一致。故事/Studio/Desk协议未迁移。
3. 主影片CLI：实际新建三个镜头的文本骨架，执行status --json、events -n 30 --json、production status --json、check，全部退出0，15份真相文件不变。没有调用build、渲染或商业生成，不把骨架当成剧情验收。

源码与安装版输出的BROWSER_RETURNED.zip逐字节一致。267份运行时文件与源码比对一致，页面内容散列246ce95818c7cfa74c77c5fc5429a5b28842099ba7304740423d969409d5e2bf。原模型catalog、quality、image_routes三份字节不变。

## 如实保留的早期问题

首次实际演练脚本错用了不存在的EDIT字段workspace.draft.prompt，改为既有shot/prompt；随后脚本用字符串wait_for_function触发页面CSP，改为函数谓词。都是测具错误，未削弱CSP，未改产品以迎合伪造数据。两份失败日志与成功运行分开保留。

初始尝试原生file导航受到沙箱拒绝，后续使用完整原始HTML隔离文档。没有把set_content宣称为原生Windows双击。

## 封包与下载门

build_ide_delivery.py必须取得终态XML/rc、冻结源指纹、实际源码/独立安装的文件与CLI回程、运行时一致性才能发布。测试覆盖假成功、缺步骤、空/重复/失败用例、页面/源码/安装不一致、同目录假安装、异常JS等情况。

正式构建阶段还将实际执行ZIP独立解压、逐文件哈希、Git bundle克隆/fsck、祖先与R2补丁重放、实际wheel比对、源快照冷bootstrap。最后从最终包安装并复演，真实五分段乱序合成、浏览器下载、选回哈希核验只在最后下载审计记录成功，不由本报告替它提前盖章。

## 边界

并未安装实际Codex/Claude Code/Cursor测试其指令遵循，没有商业模型/账号/付费调用，不保证任何模型完全内化。文档约定不是权限沙箱；原包清单不是签名。原UI未保存稿不在AI磁盘文件中，返回不是后来本地新稿的自动合并。

未执行全项目回归；历史3个Hypothesis模块的依赖缺口不在本轮宣称解决范围，Ruff未运行。Windows实机双击、完整依赖安装、退出升级回滚、固定FFmpeg、最大容量和辅助技术未全面认证。未进行整片重渲染、专业色彩或新模型画质测试。原LAST_GREEN不推进。源文件/安装/归档/下载与用户本地落盘分别报告。
