# 最终选定验证 · r16.11 重建交付

日期：2026-09-14。原始基线为用户重新上传的 r16.10，未把上一轮丢失包或聊天测试计数当证据。最终附件的大小、SHA-256、独立安装与浏览器下载结果见配套 DOWNLOAD_AUDIT.json；本报告区分代码验证与用户本机落盘。

## 终态选定回归

同一个进程执行 **1,205 个不重复测试：1,204 通过，1 跳过，0 失败**，退出码0，274.05秒。保留上版1,084项选择范围，再加入80项新功能/交付测试及额外文档/Windows源码约束检查；不能将前期或重复运行累加为覆盖率。

新测试覆盖：稳定ID三方对齐，原句/新句/本地句保留，冲突与缺失拒绝，重复接回，分批选择，不自动重新选择刚取消的字段，40项分页，结构增删与引用原子验证，本地结构不同的保守拒绝，删除对象保护，新历史与关联保持，旧简报/观察/顺序不可改，合并后容量失败不部分应用，读文件竞争、乱序回调、UTF-8/BOM/重复键/异常嵌套/无效Unicode/超大文件、实际JSON下载和CLI返回、新输出拒绝覆盖。

唯一跳过是原测试的浏览器 file:// 重载/localStorage 路径，受沙箱管理员策略限制。4条警告来自既有故意重复ZIP项和Pydantic测试，不当作功能认证。没有用删测试、屏蔽校验或放宽容量换绿灯。

终态源/测试/构建脚本共949份字节指纹冻结；后续只收口报告与状态。git diff --check无错误。终态前曾完成1,164项（1,163过、1跳过），随后修复旧文档和源码末尾格式并重跑当前完整集合，前轮不代替终态。

## 实际源码与独立安装

source-final 与 installed-final 用完整生成HTML、Playwright/Chromium、真实旧收工示例和实际命令执行：

1. 从旧收工包恢复，保存给IDE的最新基线。
2. 实际ide-open建副本，脚本改第二场动作及对白，ide-preview与ide-story-return导出实际文件。
3. 网页后来改第一场和第二场对白，用三方预览只接回动作，冲突对白保留。
4. 一次撤回、重新接回、收工保存、选回核验、新窗口恢复、视频实际播放、1920×1080原目标图查看。
5. 重复载入改稿时已应用项不重复；继续编辑使旧预览失效。
6. 原r16.10页面完整恢复并重新导出，结果ZIP逐字节一致。

两种运行环境的候选JSON、最终收工包和HTML一致；安装路径不同，269份运行时文件逐字节相同，不让源码替安装版代考。五份实际合成媒体与旧输入哈希一致，内层STUDIO.zip原字节保持。额外独立验证PNG尺寸和解码全部视频帧；这不是商业生成画质测试。

## 收口发现与修正

- 红测试首先明确复现无选择性模块，后实现；不是先写“通过”再找证据。
- 对近上限两份故事，旧workspace JSON解析器的2MiB界限不适合新封装；新增严格读取器与紧凑导出，仍独立限制每个故事1MiB，实际极限回读测试通过。
- 成功接回后不重新勾选用户刚取消的其他字段；异步旧读取不会隐藏后来先完成的有效预览。
- 桌面/手机真实截图显示文件管理占空间，载入后将管理区折叠，正文优先，未删除旧控件。
- 根README同时写旧r16.8/R13为当前版本、CLAUDE入口缺STATE指针，以及14处旧测试默认编码，均按真实当前入口与显式UTF-8修正。原失败日志保留，Windows源码约束仍正常运行。
- 一次外部审计脚本误用先前安装目录，导致相对路径比较报错；两次产品演练本身已退出0。修正审计路径后对真实269文件重新比对，通过并保存，未修改功能源码掩盖错误。

## 发布门

发布器要求终态XML/退出码、冻结源hash、真实源码/独立安装RESULT、269运行时比对一致才工作。只接受新空目标目录，拒绝旧输出覆盖。完整包独立解压后检查外层与内层清单、wheel/源码、bundle克隆/fsck、R2补丁重放和冷启动路径。最终包内wheel另装新目录，运行包内原演练脚本，不能用开发目录代替。

实际浏览器下载核验另使用本次真实完整ZIP、上手ZIP和五份分段，测试改名识别、截断拒绝、缺片/重复拒绝、乱序合成、浏览器下载、从硬盘再读并与完整包比对。是否成功及精确输出文件以最终DOWNLOAD_AUDIT原记录为准，文件不存在就不提供猜测链接。

## 未认证范围

这是选定回归，不是全部项目测试。未运行Ruff/Hypothesis全套、Windows原生双击/完整依赖安装/退出/升级回滚、固定FFmpeg6.1.1、所有商业IDE客户端或供应商实际生成、最大512MiB媒体压力和全部辅助技术验收。Linux运行时与完整HTML隔离文档不能代替原生Windows认证；没有付费、上传、自动选片或整片重渲染。原模型能力和质量名单未更新。

三方接回证明所选字段如何变化，不证明戏剧逻辑或语义依赖完全正确。packet内基线不是签名；拥有全部文件写权限的工具不是安全沙箱。它也不读取另一个窗口未保存稿。未应用JSON仍是独立文件，需保留；收工ZIP并非所有主影片/剪辑工程的备份。

所谓本轮最终版指冻结当前已验证实现与交付，不是永不出现缺陷或永不需要适配未来环境的承诺。最终落盘仍请用户用配套助手选回自己的实际下载文件。

## 文档与真实接管收口复验
最终接管入口、文档可达、AI只读启动和发布门另行实际执行，结果见 closeout.xml/rc；与终态用例有重叠，不累加成覆盖率。
