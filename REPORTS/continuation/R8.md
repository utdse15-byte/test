# R8 完整工作现场

恢复原始表单（允许未完成、保留空白）、当前能力档、原项目上下文、实际素材、未建场次候选、已有审片历史与未提交意见。
备份对所有引用取闭包并按内容哈希去重；缺文件拒绝导出。导入在隔离状态验证ZIP、CRC、SHA-256、引用和审片链，显式确认后才同步替换页面。
新素材和原项目不被修改；当前确认和型号选择不恢复，历史批准只是原场次声明。
新CLI `manju models workspace-verify archive.zip` 只读校验，不解压、不改影片。支持本工具ZIP_STORED格式，不接受任意压缩备份。
本地限额128MiB/文件、512MiB总媒体，不是供应商能力限制。

## 实测
R8与R4/R5/R6、CLI/schema约束组合：160通过、1跳过，18.29秒。见EVIDENCE/r8-core.xml。
单次初轮26通过、2个浏览器等待工具调用触发CSP限制；改用Playwright断言等待而没有放松产品CSP，原始日志保留。
真实浏览器下载ZIP、Python独立核验、恢复媒体与未完成文本、再次导出内容哈希一致。没有假称浏览器确认磁盘保存。
跳过为原有沙箱file导航限制。不是全项目回归或Windows认证。
