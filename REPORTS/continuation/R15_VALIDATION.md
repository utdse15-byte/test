# R15 验证报告

## 身份与环境

输入为实际核验的R14完整包，SHA-256 `179e736e226c737865dacd563d691119d61fea982cc7b8759779271aa3a1aa0c`，基线Git `f0df46d95e2113ba5d89bef30df518455672de50`。原包及旧快照未修改。R3/R4为功能重建继承，不是丢失原包字节级恢复。

Linux；Python3.13.5；Chromium144.0.7559.96；FFmpeg7.1.5；使用实际HTML的isolated_document浏览器传输。原生file://探针实际返回ERR_BLOCKED_BY_ADMINISTRATOR，不能称为Windows双击验收。

## 终态选定回归

8个互不重叠分组，833个唯一节点：**832通过、1跳过、0失败、0错误**。每组均获得实际退出码0。新增R15测试75项全部通过。计数以XML的class+name去重，专项与后续重复复验不累加。原测试保留，没有通过改断言、删除红项或降低门槛获得数字。

| 分组 | 总数 | 通过 | 跳过 |
|---|---:|---:|---:|
| catalog-director | 188 | 188 | 0 |
| studio@terminal | 65 | 65 | 0 |
| flex-only | 88 | 88 | 0 |
| exchange-only | 75 | 75 | 0 |
| runtime-authoring | 61 | 61 | 0 |
| workbench-review | 71 | 70 | 1 |
| delivery-workspace | 131 | 131 | 0 |
| interchange-governance | 154 | 154 | 0 |

唯一跳过项是旧页面原生file://重载限制。完整工程只完成7,082节点收集，三处Hypothesis模块缺依赖，收集退出码2；未执行全量回归。Ruff缺失未执行。尝试通过pip安装测试依赖因DNS解析失败，日志原样保留，不称安装成功。

## 原始未完成/失败记录

- `exchange-initial`：最初63项通过，之后继续增加测试，不是最终75项的替代记录。
- `exchange-hardening`：96项中95通过1失败。新竞态测试假设isolated_document有crypto.subtle，实际没有；改用显式摘要屏障同时覆盖WebCrypto/本地fallback，未改产品散列语义。`exchange-verified`98项通过，终态专组75项通过。
- `selected-regression`：尝试将833项一次执行，外层工具超时，未获得完整XML或实际终态退出码。不能把它标成同进程全绿。
- `group-studio`与`group-flex-exchange`得到完整通过XML，但外层调用在记录终态退出码前超时，未作为发布计数组。后来分拆为独立已完成组取得退出码0。不能从此断言外层中断的精确底层原因或整个环境完全无问题。
- `source-exchange`首个媒体脚本把catalog当成Studio顶层属性，实际在workspace内；按真实模型结构修正脚本，产品代码不需要补丁。验证版和独立安装版均通过。
- `project-interchange`首轮脚本误用既有apply_roundtrip参数位置，按其keyword-only与零基索引契约更正。后续真实导出、修剪应用、打包恢复全部通过。
- `old-r14-reader`首轮测试表达式被原CSP拒绝eval。改为函数表达式测试，不降低CSP。验证版实际恢复和播放通过。
- `native-file-probe`及依赖安装失败完整保留。上面各项不是“被覆盖成绿”的日志，最终结论不把未完成当完成。

## 实际运行（10组）

源码/安装版分别执行四条相同工作流，共8组：原三个工作区保存恢复；新增外部JSON/TXT与实际媒体往返；R14局部组合/个人模板；R3生成取消与恢复出片。另执行主工程实际进出链路和未改动R14读取新结果，共10组。

第09区把两段真实合成视频和三张PNG共五份媒体按原字节导出，原1920×1080目标PNG保持。实际写UTF-8 BOM/CRLF文本，经页面载入再保存JSON；同时改本地镜头，默认保留冲突。其余5项外部修改按明确选项应用，未提交意见改变而历史决定不变。随后明确接受冲突并立即撤回。更换底片后旧改稿被拒绝套用。没有使用真实影片或冒充用户批准。

Python独立核验每份导出媒体的SHA-256，对导出视频执行完整帧解码；浏览器/Python预览与应用后文档一致。新窗口恢复后视频无需重绑播放、再次导出字节一致。源码和独立安装版的材料ZIP、回传JSON、新Studio ZIP、旧组合/模板、六秒成片分别字节一致。

主工程副本实际导出10种文件（含CSV/Markdown清单与多种字幕/NLE材料），编辑本项目FCPXML入点/时长、显式接受回程修剪，再pack --full/unpack，69个恢复文件与归档一致。原合成工程不变；未在Resolve/Premiere/剪映应用导入，不认证其全部语义。

原封不动R14的Python验证器、原HTML读取R15改稿后Studio/v1，视频播放且重新导出字节一致。新外部编辑协议不要求旧R14理解，但最终现场继续用旧Studio格式。

## 源码/安装隔离与交付门

实际独立wheel安装在源码目录之外，导入路径明确来自installed/manju。262个运行时文件与受测源码逐项一致；新HTML与tools版本一致。96个受保护文件（媒体、供应商、构建模块及模型数据）与R14逐字节一致，十二档与质量名单未变。

`EVIDENCE/R15_VERIFIED_FACTS.json`是选定范围和演练汇总；`TESTED_RUNTIME_SHA256.json`固定受测运行时。封包前后都要复核该表，测试之后的文档收口不改运行时代码。交付本身另由VERIFY_PACKAGE、新目录解压、wheel/source比对、Git bundle恢复、阶段祖先和R2补丁重放验证；最终精确文件/提交计数读PACKAGE与独立receipt，不在封包前编造。

## 明确未认证

不是全项目回归全绿；不是Windows实机安装、原生双击、退出、升级回滚或固定FFmpeg6.1.1验收。LAST_GREEN不推进。商业API调用为零，未做真实画质或费用排名。

R15是数据编辑/交接，不是自动执行。128 MiB单文件/512 MiB去重素材/16导演锚点仍在。EDIT.json最多2 MiB；固定字段最多30,000 UTF-16单元，未提交审片6,000，锚点目标4,000。模板/交换/备份都不能把不受支持的外部软件工程变为无损通用格式。交付文件存在与完整不代表用户已下载到设备，不承诺永久托管。
