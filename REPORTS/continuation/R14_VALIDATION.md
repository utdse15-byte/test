# Manju R14 验证与交付边界

## 结论与范围

R14是在真实R13 `7284f4716920243fd9e341f3d35ad272975b490c` 基础上完成的组合与个人模板候选。此报告记录已经执行的源码/安装版测试；最终下载身份另由PACKAGE与独立恢复收据确认，不从报告推导已在用户电脑落盘。

## 终态回归

`EVIDENCE/selected-release.xml`与`selected-release-run.json`：同一个pytest进程、29个模块、**720个不重复测试点，719通过、1跳过、0失败、0错误**，真实退出码0，墙钟117.88秒。新增R14模块88项全部通过。旧632节点保持在本轮范围内；重复运行不累加成覆盖率。

覆盖R2草稿/收尾/交接、R3运行取消、R4至R13离线工作台与恢复、R14组合/模板、CLI兼容、协议、付款确认、Windows源码守卫与零费用策略。这不是完整项目全量回归。

唯一跳过：`tests.test_rebuilt_r5_workbench::test_edits_invalidate_and_draft_recovers_without_approval`，沙箱阻止原生file://重载与localStorage路径。isolated_document执行原始页面不能证明Windows原生双击通过。

新增项覆盖全部15种工作区组合、未选部分保留、目录单独选择、媒体引用闭包与共享资源限制、模板白名单和纯文字模式、没有素材时的编辑/撤回、定稿意图失效、非法JSON、输入中途被替换、有界复制、拒绝覆盖输出、异步读取冲突、过时预览/模式/选区、撤回保护、命名ZIP与下载回验、390像素窄屏、浏览器无法原样表示的数字/换行提前拒绝。

文档与演练脚本收口后，另对CLI/协议/文档/交付入口重跑91项，全部通过；与上面的720范围重叠，不相加。运行时冻结哈希未变化。

## 真实媒体与安装隔离

`EVIDENCE/FINAL_FLOWS.json`逐步记录八项实际演练的命令、退出码和时长；每一项真实退出码均为0。源码版和独立pip目标目录安装版分别执行：

1. 原三工作区总备份：实际合成视频、未完成文字、候选审片记录、高像素PNG、下载/选回/新窗口恢复与重导。
2. R14组合与模板：A方案保留新镜头和返工；只从旧总备份取导演材料。B结果含5份去重素材、892744字节，保留1920×1080目标图与底片；两段视频恢复后直接播放。预览后哈希、Python组合文档与浏览器保存的STUDIO.json一致。
3. 原生制作：生成中取消、保留已完成候选、恢复生成、明确选择合成候选、六秒三镜头出片、缓存复用、再次取消编码与旧片保护。此选择仅是合成夹具操作，不替用户批准真实影片。
4. 实际Chromium截图取消：观测自有截图子进程、取消并保护旧封面，独立观察进程仍存活。不是截图渲染成功声明，也不是Windows进程树认证。

源/安装两版使用的261个运行时文件逐项相同。下载的Studio、A/B命名备份、恢复重导、C模板应用后备份和模板JSON分别逐字节一致；六秒成片SHA-256为`0fa51fdb5b2acff13bf0774f3ec57d181055a815b9b1fdac3822cb4dcb4fab49`，两版一致。PNG原文件没有缩小或转码。演练记录HTTP请求0、页面错误0、商业生成0。

**另使用未修改的真实R13核验兼容。** `legacy-r13-verified/RESULT.json`记录从旧R13独立source导入的0.2.0+r13 Python验证器，以及哈希匹配旧PACKAGE的原HTML。旧页面真实恢复R14的B组合包，视频直接播放，重新导出的ZIP与输入逐字节相同。不是将R14页面改标题冒充旧版本。

## 未被抹掉的早期记录

`r14-first`74通过，`flex-studio-second`160通过，`r14-third`82通过，`r14-text-fidelity`88通过，`selected-owned`715通过1跳过，均为中间或重复范围，不与终态相加。

`related-second`曾有122通过1失败：新增页面已显示R14，而交付版本文件尚为R13。版本元数据随后更新；终态兼容测试通过。`related-first.log`和`selected-final.log`曾被单次工具执行时限打断，未取得真实终态，不计入通过。此后改为本回答内拥有实际进程、完整日志/rc/XML的运行方式，终态记录独立保留。

初次实际媒体演练后发现媒体清单排序导致预览哈希与实际导出不同，按原引用顺序修正，并在终态脚本增加实际文档哈希等式断言。早期输出保留为诊断，不当作修复后证据。

额外的旧版兼容演练初次失败于测试脚本错误地假设HTML的title带R13；旧标题并无版本字符串。脚本改为校验旧包清单哈希、独立Python版本和实际来源路径，再完成真实播放与重导。初次失败在`legacy-r13-real.log`原样保留，不是生产功能失败的隐藏重跑。

## 保留与交付门

`PRESERVATION_CHECK.json`确认：原260个运行时文件无删除；只更改版本号、作者CLI、嵌入页面，新增flexibility.py。核心媒体、取消、工程写入、十二个能力档、质量名单均未改动。176个原CLI命令签名保留，新增4个；21份原项目/任务快照保持字节不变。

`TESTED_RUNTIME_FREEZE.json`固定已测运行时字节。封包前必须再次比较源/安装/页面，提交后只从干净Git树封包。完整包包含源代码、wheel、完整历史、从R2的补丁和EVIDENCE；构建器检查字体排除、CRC、逐文件SHA-256、wheel/源码比对、Git fsck、阶段祖先与补丁重放，再在全新目录解压独立复验。已有下载名或收据存在时拒绝覆盖。

封包完成后以`MANJU_R14_CUMULATIVE.receipt.json`和下载审计记录为准。文件完整性不是数字签名，不替代软件回归或用户本机落盘确认。

## 未关闭门

全仓仅做收集：7007个节点，3个模块因缺少Hypothesis报错，退出码2，**没有执行全项目回归**。模块为test_crash_safety_campaign.py、test_fp_timebase.py、test_properties.py。Ruff与Hypothesis未安装，不宣称已运行。

环境Linux、Python3.13.5、Chromium144.0.7559.96、FFmpeg7.1.5。Windows实机安装/启动/退出/原生file://、升级回滚、固定FFmpeg6.1.1、跨平台大媒体与真实商用生成仍未认证；LAST_GREEN不改动。

本轮不新增模型能力排名或商业API，不运行新模型画质盲测。现有质量名单是原有日期证据，模板或工作区组合不能授予型号新能力。无用户长期亲测，不宣称使用效率提升比例。

总备份只覆盖三个离线工作区，不包含整部影片工程、未绑定素材或外部剪辑项目。组合是整块取用，不是多历史深合并。单文件128MiB、去重媒体512MiB和16锚点等原边界不变；没有压低素材质量来绕过资源上限。单步撤回为当前页面暂存，不是自动备份、长期分支库或跨设备同步。
