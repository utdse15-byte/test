# R16 验证报告

本报告只按本轮实际日志记账。选定范围通过、源码/安装一致性、交付文件可恢复是不同的门；不互相冒充。未推进Windows正式绿灯。

## 终态选定范围

| 范围 | 节点 | 通过 | 失败/错误 | 跳过 | 证据 |
|---|---:|---:|---:|---:|---|
| 终态单进程选定回归 | 898 | 897 | 0 | 1 | `selected-corrected.xml/.log/.rc` |
| 最终接管入口 | 4 | 4 | 0 | 0 | `entry-final.xml/.log/.rc` |

两组节点不重叠，合计902个不重复测试点，901通过、1跳过。69项R16新增测试全部通过，已包含在898内，不重复累加。包含R3至R16选定用户/媒体/协议/交付检查，以及有限时间线回程、外来计划、工程打包/契约/CLI兼容检查。精确命令和节点在EVIDENCE；不是全部项目用例。

## 原始失败，没有覆盖掉

`r16-initial.log`首轮没有完成（没有完整XML/退出码）。随后`r16-first-failure`为38通过1失败，测试向未展开的模板details内部输入而超时；改为真实点击展开，不用强行写DOM绕开交互。`r16-second`63通过；新增更多异常/键盘/竞态覆盖后`r16-third`69通过。

第一次扩大回归`selected-regression`：898节点，895通过、2失败、1跳过。两项旧测试明确期待“无效替换后清空旧材料”，与新防丢稿行为冲突。本轮更改了这两项的行为断言，并加强检查旧有效材料保持、非法字段/脚本不接受、旧预览和确认清除；没有只删除安全测试。还修正错误后仍显示旧“已加载”的误导提示。`import-rejection-fix`2通过后，重跑898全组取得上述终态；前一份失败XML保留。

## 真正的文件与媒体演练

`release-source-desk`和`release-installed-desk`均完成：原Studio通过统一入口恢复、外部改稿及模板分流载入、保留本地新稿、收工包实际浏览器下载、选回核验、新窗口预览/确认恢复、再次下载。五份原始素材字节不变；两个不同MP4在恢复后可播放，随后FFmpeg分别完整解码。4个视频控件引用这些实物并实际推进播放。1920×1080目标PNG不缩小。待接受意见和模板原样保留，但外部冲突默认不勾选，未自动覆盖本地。

源与独立安装版收工包逐字节一致，各自新窗口重导出也一致。未改动的R15页面和Python验证器实际核验内层STUDIO.zip；原R15页面能恢复播放并逐字节重导出。不是仅检查版本号。父目录输入BASE_STUDIO/RETURNED_EDIT原字节保持。

`release-source-runtime`与`release-installed-runtime`均运行合成三镜工程：首个真实候选注册后取消，保留候选并恢复生成，显式选择仅针对合成测试，输出六秒成片，再取消20秒编码并检查旧成片保护。源/安装版六秒片逐字节一致；取消返回分别约0.671秒、0.616秒，只是此环境观测，不承诺所有机器。实际完整解码通过。

独立安装仅使用本轮构建wheel，不借源码目录代考；receipt记录实际manju路径。263个运行时文件逐项与源码一致。94个媒体/供应商/构建及目录/质量名单文件与R15保持；24个旧状态/任务文件及原R15ZIP不变。保护清单和所有运行时哈希见`runtime-preservation.json`。

## 环境与未完成的门

Linux/Python3.13，本轮FFmpeg与Chromium版本见environment.json。无Hypothesis和Ruff。全项目仅执行收集：7151个节点，另3个模块因Hypothesis缺失报错，退出码2；没有把收集当成执行。Ruff未运行，Python编译、JS语法和git diff --check另有退出码记录。

Playwright执行最终原始HTML与真实媒体下载，但使用isolated_document隔离文档方式，原生file://实际探测被ERR_BLOCKED_BY_ADMINISTRATOR阻止。唯一跳过为既有原生file重载场景；不把它改成伪通过。Windows实机安装/双击/退出/升级回滚、固定FFmpeg6.1.1、商业供应商执行均未验收。浏览器离开提示不是可靠自动保存，未做大媒体上限内每一种真实设备压力认证。

本轮没有模型质量排名复测、商业生成调用、付款、账号访问、真实影片工程改写或自动选片。结构校验不证明素材权利、质量、身份签名或客户端永久存盘。

## 交付门

版本源码与报告先提交，干净Git树才封包。完整ZIP只有经过新目录解压、所有文件哈希、wheel源码比对、Git bundle恢复/fsck、阶段祖先与R2补丁重放后才排他发布；不覆盖旧下载。最终准确提交/大小/数量以外部receipt为准。交付wheel重新构建后还必须与本轮受测独立安装版的263个文件逐项比较，不能只看版本号。

完整包、小工具和受测页面必须原字节一致，下载文件重新核验CRC/SHA256/路径/成员；原R15包校验值仍为`312823ef257c2044ca7873fbad19f8984eff5bae261ad671002f06196b300a7b`。这些都不代表已经保存到用户电脑，请实际下载并在本地核验。
