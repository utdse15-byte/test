# Manju R11 验证与交付边界

核验日：2026-09-08。输入为真实R10归档，SHA-256：
`6b895783ec81903714bfaf722169e37733728b180397ec50457995230a38afc4`。
R10基线提交：`be5923a58005dd85e18f593134f853d886c69a2b`，797个提交。

R11是累计修复候选，不是Windows正式绿灯，也不是商业执行已接通。历史R3/R4是功能重建，不是原丢失ZIP字节复原。旧文件与状态未覆盖。

## 本轮代码范围

新增独立返工计划、ZIP、浏览器工作区、实际原片重新打开及CLI核验；新增Seedance2.5 BytePlus LAS作者档；新增显式像素尺寸的v2返回报告。默认Request/v1、旧九个能力档、返回报告v1与工作现场/v1保持兼容。

返工区单独备份，不改变旧工作现场协议。毫秒范围不是帧精确剪辑。原视频不剪切、不上传、不覆盖；不自动调用模型或拼接。ZIP核验验证字节与声明一致，不独立重探视频元数据。元数据测量来自浏览器绑定或CLI创建时的本地探测。明确像素尺寸不证明供应商来源或画质。

## 终态选定范围

| 分组 | 节点 | 通过 | 跳过 | 失败/错误 | 退出码 |
|---|---:|---:|---:|---:|---:|
| 协议登记、CLI、发布工具、R4作者层 | 121 | 121 | 0 | 0 | 0 |
| R5/R6/R8/R10浏览器与旧现场兼容 | 137 | 136 | 1 | 0 | 0 |
| R11返工/精确尺寸与R9返回检查 | 126 | 126 | 0 | 0 | 0 |
| R2保存交接、R3取消、R7入口与计费边界 | 77 | 77 | 0 | 0 | 0 |
| **不重复总计** | **461** | **460** | **1** | **0** | **全部0** |

91项新增测试全部通过。节点清单、各命令与运行时文件指纹在EVIDENCE/FINAL_TEST_STATUS.json。重复运行不累加覆盖率。最后一次代码检查包括git diff --check和相关Python编译，均退出0；未运行Ruff，不宣称静态检查全绿。

完整项目仅执行了收集：6795节点，另有3个模块因缺Hypothesis报错，退出2。不能把收集数算成执行通过数，更不能说完整回归已通过。

## 实际媒体与浏览器闭环

源码版与独立安装wheel均使用真实Chromium执行原始HTML，完成：六秒1584×672合成视频绑定；1.201至2.444秒返工范围；上下文0.901至3.044秒；下载含实际原片的ZIP；新窗口核验恢复并播放，无需重新选原片；恢复不覆盖另一份主任务，当前返工确认清除；原片哈希不变。

同一真实合成视频在默认v1短边规则下报告needs_attention，在明确1584×672的v2检查下匹配；原任务哈希不变。浏览器保存两种报告，Python另执行全部视频帧解码。全程没有审片批准记录产生或自动选片。

两版的HTML、合成视频、返工ZIP逐字节一致。安装路径实际指向独立的installed目录，而非源码目录；256个wheel运行时文件与源码/安装目录逐项比较通过。

两版另执行R3本地链路：生成中取消，保留一个已经注册的候选；恢复生成，合成测试中明确选片；六秒三镜头出片；再次编码中取消，旧成片不变；再次构建缓存复用。两版六秒成片逐字节一致。这些选择只针对合成测试，不冒充用户对真实作品的批准。

本轮观察到真实Chromium截图取消约0.608秒返回，3个观察到的自有进程停止，旧封面字节不变，独立观察进程仍存活。该数值是当前Linux环境的一次观测，不是所有机器的时延保证；也不是成功截图或Windows进程树验收。

## 原始失败和未完成项

r11-first记录71通过/1失败，原因是测试以字符串轮询触发CSP unsafe-eval限制。后续改为函数谓词，没有放宽页面CSP。旧R5测试中相同写法在一次组合运行中再现，已统一修正该类测试和演练写法；r11-browser-final原始1失败/135通过/1跳过记录保留。

直接file://导航在本沙箱被管理员策略阻止。初次浏览器组合未配置isolated_document，72通过/65错误，错误来自同一导航限制；该次与另一次外层执行超时的原始日志保留，缺失进程退出记录不补写为成功。终态运行另有四个实际退出0的结果，未覆盖原始记录。

依赖安装尝试未成功。第一次本地wheel命令错误地将相对名称repo当成包名，构建与安装失败日志保留；改用明确本地绝对路径、--no-index后成功构建/独立安装。之后封包构建也采用本地禁止索引路径。

Windows实机安装/双击/退出/升级回滚、固定FFmpeg6.1.1、完整属性测试与全项目回归尚未完成。当前浏览器传输为isolated_document，不是file://或Windows实机通过。商业调用、画质比较、实际节省费用、本地LTX推理、云端取消和计费确认均未执行。未推进LAST_GREEN。

## 交付核验

最终包由干净Git树构建。发布工具要求：源码清单无字体，完整历史对象路径无字体；完整Git bundle；由R2到当前的二进制补丁；wheel逐文件一致；新目录ZIP解压；CRC与所有文件SHA；Git克隆/fsck；R3至R10各阶段祖先关系；从R2补丁重放得到当前树。所有门通过后才创建最终下载文件，旧归档不覆盖。

本报告不预填尚未产生的归档哈希、提交或文件数。确切交付身份与实际封包结果以包根PACKAGE.json、SHA256SUMS.json和下载旁的MANJU_R11_CUMULATIVE.receipt.json为准。完整包、免安装小包的HTML另作字节比较。文件完整性不等于Windows验收，也不能代替用户本地浏览器下载列表中的保存确认。
