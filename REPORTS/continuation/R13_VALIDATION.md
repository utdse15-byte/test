# R13 验证记录：个人创作台与三工作区总备份

日期：2026-09-08。候选版本 `0.2.0+r13`；实际R12基线 `d7a46588d23e3dda375d83016c1d02b2f9a8ac0a`。最终提交和归档身份以 `PACKAGE.json` 与独立恢复收据为准，不提前虚构包哈希或封包文件数量。本报告不是正式全绿发布证书。

## 终态选定范围：632个不重复测试点

`combined-owned.xml` 是单个进程完成的实际运行，`.rc`为0，`.completion.json`证明写出Junit结果。总632，631通过、1跳过、0失败、0错误，包含65项本轮新测试且全部通过。执行器实测耗时108.85秒，pytest自身报告106.31秒；耗时不是其他机器的性能保证。

选定的28个模块见 `selected-tests.txt`，涵盖CLI/协议/付费与Windows静态守卫、R2创作/音效/信息卡/素材交接、R3取消与R7交付、R4-R6作者交接与审片、R8-R11恢复/返回/能力档/返工、R12质量与导演、R13统一备份，以及文档命令检查。`FINAL_SELECTED_TESTS.json`逐节点列出结果，重复跑的分组未累加。

唯一跳过是原生 `file://` 重载与localStorage恢复测试：当前沙箱阻止该导航，未改写为通过。其余浏览器测试执行实际自包含HTML的 `isolated_document` 路径，不是Windows原生双击认证。

新测试覆盖：原样保存空白/未完成表单、未提交审片意见与待定稿分辨率、三域素材闭包/去重、Python与JS规范化/哈希一致、恶意ZIP路径/前后缀/目录矛盾/压缩/重复成员/重复JSON键/元数据越界/篡改、没有真实文件时不产生空壳、只读CLI错误码、只核验下载不替换、部分JSON导出不清除总备份提醒、预览期间编辑与并发异步导入拒绝覆盖、质量优先恢复、清除当前确认、窄屏布局、最新交接状态指针、核验期间文件变化拒绝出具旧结果和首页错误反馈。

## 真媒体与独立安装版

最后的运行时代码修正之后，重新构建wheel并安装到独立 `installed-final` 目录。260个运行时文件与源码、受测wheel逐项一致。第三方依赖仍来自当前测试环境，不冒称全新系统安装；安装版导入路径由每份演练记录明确记录，不借源码目录运行。

最终8组真实媒体演练是 source/installed × R13、R12、R3、Chromium取消，所有`.rc`均为0。原始记录在 `*-postcheck/RESULT.json`、对应日志及 `FINAL_SOURCE_INSTALLED_PARITY.json`。

**R13跨三工作区：** 真实合成两条640×360、3秒H.264/AAC视频与一张1920×1080目标PNG。一个原视频同时用作镜头素材、审片候选、返工底片和导演底片，另一个作为审片候选。建立两个导演参考帧，只绑定一个高像素目标图；第二个目标和导演镜头名保留未完成状态。返工范围刻意暂为2.444秒至1.201秒的倒置草稿，明确不是可执行请求。记录一条合成`revise`审片决定，再保留下一条未提交意见。

浏览器真实下载总备份后，由独立Python核验得到5个去重媒体、892744字节；选回本地文件核验不改当前现场。另一个窗口先有自己的文字，导入预览不会覆盖，明确确认后恢复三个区，视频直接播放，不重新绑定。两个候选和高像素PNG的字节不改写，所有原始文字与待定稿输入保留；当前确认和选择清除，质量优先开启。恢复后再次导出ZIP逐字节一致，源码版与安装版ZIP也逐字节一致。两个视频另行执行FFmpeg全视频帧解码通过。浏览器没有页面错误或HTTP请求，390像素宽页面无水平溢出；最终窄屏截图另行目视检查。

**R12兼容：** 旧独立导演ZIP的导出、恢复播放与质量筛选演练重新通过；1920×1080目标图未缩成底片尺寸。源码版与安装版旧导演ZIP身份一致。旧Workspace/Repair/Director v1协议和其黄金测试保持。

**R3本地制作：** 第一条本地占位候选真实完成后取消，完成候选保留；恢复生成后停在人为选片门。只在合成测试项目里明确选择候选，得到六秒三镜头成片，再构建复用缓存。取消编码不会改写旧片；源码版与安装版六秒成片逐字节一致。这不是商用模型画质测试。

**真实截图取消：** 观察到本程序的真实Chromium截图子进程后取消，所观察进程停止、旧封面原字节保留，独立观察进程继续运行。该记录不宣称截图渲染成功或Windows进程树通过。

## 原有内容保护

原R12 ZIP SHA-256仍为 `9edbc097c2362f44f12eb6aa749280f2f73ddaadf0b9c3f33635dc16c2977529`。R12源码中的18份旧状态/任务/绿灯文件逐字节保持；旧ZIP没有覆盖。目录12个能力档和质量名单文件字节保持，没有新的模型质量结论或SDK依赖。

原175项CLI快照逐项保持，仅新增 `models studio-verify`，最终176项。新增两个Studio实验协议登记，不修改旧协议版本或批准哈希意义。后续最终受测源文件一致性及阶段继承由机器收据验证，不依赖手工描述。详见 `PRESERVATION_CHECK.json` 与 `TESTED_RUNTIME_FILES.json`。

## 早期红项与未终结运行保留

新入口红测试先证明统一保存/核验入口缺失和状态指针过时；首个实现有重复导航入口，已修正。第一轮功能测试对CLI错误通道误写为JSON，实际既有契约是stderr文字+退出码2，修正测试而不改旧错误协议。返回规格折叠面板未展开导致测试填写等待超时，已改为真实展开操作，没有放宽产品验证。

后期两项先红测试复现了首页不显示导出错误、只读核验中同一路径文件变化仍可报旧结果。已分别修复并进入65项新增回归。其他早期成功运行和开发中示例保留，但不作为最终字节身份依据。

两次组合工具调用未得到完成的XML/退出码，日志保留，不将已打印的点号算作通过，也不宣称它们是已定位的产品故障。改由单独的执行属主等待实际进程退出并写出结果，得到上述632个节点的终态；没有通过拆分结果掩盖同进程未完成。所有发布判断都等待文件和退出码，不使用运行中的日志代替结论。

## 明确未通过的门

全仓最终只完成收集：6919节点被收集，3模块因缺少Hypothesis报错，退出码2。没有执行全部6919项，三个模块为 `test_crash_safety_campaign.py`、`test_fp_timebase.py`、`test_properties.py`。依赖安装尝试的可用索引没有提供安装版本，日志为 `dev-deps.log`；不猜测进一步网络原因。Ruff未执行。

本环境为Linux、Python3.13.5、Chromium144、FFmpeg7.1.5。固定FFmpeg6.1.1及Windows实机安装/原生双击/进程树/更新回滚仍未验收，LAST_GREEN未推进。Windows静态守卫通过不替代实机。

Studio CLI只验证内容闭包、CRC/哈希、声明一致性和有界PNG结构，不完整解码PNG、不重探视频；浏览器恢复另外复测审片候选及返工/导演底片的视频元数据、解码导演原帧/目标PNG。没有这类元数据声明的其余镜头附件只做字节/哈希核验，不冒称所有媒体都解码过。合成演练的全视频帧解码是额外证据，不扩张CLI承诺。SDR参考帧不是HDR母版，毫秒不是认证帧号。

没有后台自动保存或网络同步。浏览器下载回读证明选择到的文件有效且与当前现场一致，不证明用户设备永久保存，不代替影片工程/外部工程的独立备份。没有商业生成调用、真实用户作品盲测、费用/速度排名或模型权重下载。

## 真正的发布门

源代码和文档须先提交且工作树干净。完整包通过文件哈希、wheel/源码比对、Git历史恢复与fsck、各阶段祖先关系、从R2补丁重放、ZIP CRC以及新目录重新解压复验后才排他创建。旧包不能覆盖。小工具与完整包页面逐字节核对，实际示例来自最终真实媒体演练并另行核验。

报告中的运行与封包是两类证据。最终文件数量、Git提交和下载SHA-256见独立`.receipt.json`与下载审计，不把本报告或测试收集当成归档已存在的证明。
