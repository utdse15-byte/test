# PROJECT_STATE

快照：2026-09-14_10-44-29 UTC。只有最新时间戳状态有效，旧状态和任务不修改。

## 目标、恢复事实与阶段
用户确认上一轮选择性取回包没有保留下来，可靠基线只有重新上传的 MANJU_CHANGES_FULL(1).zip。文件56973720字节、SHA-256 8f9d638f0ff5c4cb1d18ee440bd651d4a8f2d3d7c53d1fd8fd2bf682bbbd3b95；CRC、路径及Git历史已核验，源码基线80ca2e0/r16.10。没有从不存在的r16.11包继承代码或测试。本轮重新实现，候选0.2.0+r16.11，maintenance_revision=restored-selective-return-final；交付前冻结，最终文件以PACKAGE与下载审计为证。本状态不能提前证明文件已经发布或用户本机落盘。
个人本地影片制作，Windows11优先，用户会长期让IDE AI参与制作，要求界面舒展、顶尖质量、实际可下载。R3/R4是功能重建；早期丢失R17及长篇尝试未恢复，不因新版本号混称继承。

## 本轮有效决定
1. 新派生 manju.story-return/v1 保存完整验证的导出base与candidate及hash；manju.story-return-preview/v1绑定当前本地故事、packet和每项状态。数据是作者资料不是指令，哈希不是签名或沙箱。原Story/v1、Desk/v1/v2、Studio/v1、主影片文本格式不迁移。
2. 项目title/form/intent/ending，人物依据name/text/provenance，场景episode/title/purpose/action/dialogue/before/after按稳定ID和整字段旧值三方比较。当前=基线可接回；当前=候选已一致不重复；双方变更或对象缺失则保留本地，不按同名/同句猜对象。
3. 人物/场景增删、排列、引用与参考职责、知情范围和新增brief使用一个保守结构组，默认不选。新增删除对象完整文本纳入前提，不能删掉本地新稿；存活对象未选文本保持。旧简报与观察以及旧简报次序、project_id和link在候选不可改；当前本地历史和link在文字接回保持。这个保守组不是完整语义依赖图，结构正确不代表剧情正确。
4. 所选修改先在内存组合，再验证全文引用/长度/容量/可填写性，成功后一次赋值。合并后超1MiB整次失败，原稿不动，不截断原文/历史。源与浏览器算法逐字段对齐；2个各1MiB故事的封装上限2MiB+4096，独立严格读JSON，不受旧workspace元数据2MiB误拦。导出紧凑canonicalJSON，不因缩进使自己读不回；BOM返回支持，重复键/无效Unicode/非有限数/过深/过大拒绝。
5. 预览后的任意现场编辑使旧选择失效；文件异步乱序不让旧回调盖掉新结果；坏新文件解除旧确认但保留当前作品和上一份有效packet。网页只比较当前快照，不读取另一窗口未保存输入，不承诺共享文件CAS。CLI末尾复核实际输入，只排他发布新文件，不覆盖旧包。
6. 已接回项不重复，接回后不会把刚才没选的其他ready字段又默认选上。接回后有一次受deskGuard保护的撤回；继续编辑后旧撤回点不能覆盖新输入，也不会复活清除的批准/型号选择。明确的已采用故事照旧保存收工包。
7. 入口只在现有故事区可展开卡和统一intake，不新增导航。默认正文优先，载入后折叠文件按钮，冲突完整三方原文可读。40项分页，所选计数覆盖所有页，结构单独勾选。桌面双列、窄屏上下，原控件唯一，不手改生成HTML。

## AI与使用路径
先AGENTS.md、tools/ai_bootstrap.py --json、最新状态、docs/ai/OPERATIONS.md，再按任务读Skill。主影片实际project.yaml/series.yaml目录、软件源码、离线收工ZIP不同；未保存网页不是IDE输入。APP/source为无.git快照，完整历史在APP/repository.bundle，开发时克隆到新目录，不初始化空历史。
原ide-open创建STORY.json/EDIT.json和只读素材；ide-preview的--html保留。新增ide-story-return按最新preview_sha256导出STORY_RETURN.json，只含故事，不含EDIT文字/媒体，并输出excluded_edit_fields。镜头/返工/导演仍走原EDIT三方回程。原ide-return整包返回保持，不能当选择性合并。新故事没有同一基线时沿用原显式整本导入，不伪造身份。
独立story-return-preview及story-return-apply用于保存好的CURRENT_STORY.json；后者--take指定字段/structure并指定新输出。未接受packet独立保留或另存，不加入Desk缓冲。它包含完整作者秘密与历史，分享前检查，不是备份。已接回故事仍随原Desk/v2与可选续作保存，原r16.10可恢复。

## 既有创作边界继续有效
显式作者/观众/人物知情，人物视角不自动继承其他场景；自由文本/素材不能自动防剧透。简报固定内容与真实顺序，引用改变标复核，不重写历史。不引用的资料不牵连无关简报。当前关系仍是资料类型加正文，不是有两端ID的完整图谱。
系列Bible只读导入不是持续同步，不反写主影片工程。源码/报告/SQLite不是作品媒体。历史hash不是实际文件，重要原素材必须绑定或另存。
无故事Desk/v1，有故事Desk/v2，内层Studio/v1不变；收工存故事、三工作区、原待处理EDIT/模板，不含主影片工程/外部剪辑工程/未绑定文件/新pending-return。旧r16.10可读本轮已接回收工，旧页不支持新返回文件入口，不能说全部版本都理解新协议。
只用顶尖质量，不为草稿/省钱/速度降档；原12视频能力档、quality和image_routes本轮原字节，不重查市场排名，不作最新型号主张。商业生成/上传/付费/审片/选片/锁片分开，绝不伪造观察。AGENTS是约定，不保证任意IDE遵循。LAST_GREEN不推进。
普通HTML免安装；CLI要Python3.11+与依赖，媒体另需FFmpeg。可选续作须主动开启，每窗新点成功后才清旧点，保留3份；本机续作不等于独立备份。旧文件不覆盖，用户落盘需选回核验，附件不是永久存储。
原单媒体128MiB、总512MiB、16导演锚点；故事1MiB、512依据/512场/128简报等限制保留。Fable60场容量探针不代表用户实战失败；长篇去重未做，未来必须保留实际旧内容，不能只留hash或默认删未采纳版本。无字体/凭据/完整第三方书籍分发。

## 验证与未认证
基线55项实际重跑；新增失败测试先复现缺功能，随后核心/CLI/浏览器测试及真实5素材往返独立执行。源码与独立安装269运行时文件比对，二者SELECTED_CHECKOUT和packet字节一致；最终包解压后安装及真实下载/分片还须认最终审计。完整选定回归及准确数量见REPORTS/final_selective/VALIDATION.md与EVIDENCE/regression-summary.json，不引用旧对话数字，不累加重复测试。
没有运行所有项目测试、真实商业IDE模型推理、Windows实机双击/完整安装/退出/升级回滚、固定FFmpeg6.1.1、全部辅助技术/最大媒体负载或商业生成。成功浏览器测试为完整原HTML隔离文档，不能代替Windows原生导航。代码和包的完整性不是永不出错、永久可下载或未来平台兼容保证。

## 档案
- TASK_重建选择性接回_2026-09-14_10-44-29.md；REPORTS/final_selective/README.md、PROJECT_REVIEW.md、VALIDATION.md。
- src/manju/authoring/story_return.py、ide.py、cli.py；tools/workbench.storyreturn.*。
- tests/test_story_return*.py、test_final_delivery.py；原r16.10模块集合回归保留。
- tools/user_ready/rehearsal_story_return.py、build_final_delivery.py，终态原日志、源码hash与安装一致性。
- 旧TASK_可读故事差异_2026-09-13_17-53-56及Fable讨论、IDE接管档案原样保留。

## 假设
用户具体IDE品牌、真实并行频率、长篇规模、Windows依赖环境与最终本机落盘情况未知。未因这些假设建新常驻服务或给未经验证的全流程保证。

## 变更日志
2026-09-14_10-44-29：从用户重传r16.10重建选择性接回，进一步修复异步读取乱序、隐式重选和双故事文件上限边界；旧协议/媒体/历史不改，发布只认真实文件审计。
