# PROJECT_STATE

快照：2026-09-12_12-30-33 UTC。仅最新文件有效，旧快照原样保留。

## 目标与阶段
用户优先实际创作、灵活外部往返、只使用顶尖模型，以及可以真正下载、解压、恢复的文件。不得用版本号或聊天断言代替实物。
当前维护版 0.2.0+r16.2，阶段仍 R16，基于已恢复 R16.1 提交 00ba58ae970c3f75333f1b096dce57d7b5517e68。此前原 R17 只有截图，本轮是重新实现可选本机续作，不是原包字节复原。R3/R4仍为功能重建。最终提交与文件哈希读 PACKAGE.json 和生成后的交付收据。

## 有效决定及理由
保留直接 HTML 无 Python 的离线入口和只读本机备用入口。新增 Python 标准库可选续作服务，用户显式开启才写用户主目录 ManjuRecovery。恢复目录与软件解压位置分离，不依赖浏览器缓存。每个点为已有 Desk/v1 收工 ZIP，可返回旧 R16，不锁定新格式。
本机点不是独立备份。工作台恢复 API 增加 source=file/local_recovery 和 restoreSourceVersion:1；来源本机不设置已核验独立备份状态，避免依赖事件顺序清除标记。当前确认勾选清除，质量优先恢复。旧页面和新服务混装拒绝。
随机环回地址、路径、令牌，严格 Host/Origin 和固定路由。只写明确恢复目录，不开放影片目录或执行上传。新点先完整写入、flush/fsync、读回哈希、原子发布，再清理本窗口旧点。跨进程目录锁和独立窗口序列防交叉覆盖。损坏、额度、权限或中断失败保留旧点。
空闲约8秒、保存结束后最少30秒间隔；关窗瞬间不作为保存保证。窗口关闭开关后，尚未发送的写入不继续；已经发出的写入可能完成。pagehide后恢复观察器通过模拟事件验证，不是原生导航。

## 长期约束
原影片/旧ZIP/旧状态/旧任务不覆盖，LAST_GREEN不推进。没有自动模型选择、降档、付款、云上传、生成、选片或锁片。原十二个能力档和质量名单不变，未做新模型排名。主影片工程/外部NLE与离线台分离。模型远程计费不从本地取消推导。
媒体单文件128MiB、去重总量512MiB、16导演锚点不变；恢复点最大520MiB，总目录2GiB、64点；同窗口最多保留3个已完成点，不删除别的窗口。新点需先有完整空间，额度满先暂停，不能先删旧点腾空间。
Desk范围仍是三个区、已载入未接受改稿/模板和文本；主影片、未绑定素材、未应用能力档/组合预览、临时撤回、播放器位置不在其中。没有加密或签名；不作为公网服务。标准库入口不是完整影片依赖安装器。

## 本轮实证与遗留
371项终态选定回归同进程通过，包含49个新增运行时测试；10项新增封包测试另行通过；文档入口复验单独记录，不能累加为全项目。4组源码/安装版真实媒体演练通过：服务重启后恢复5份媒体及缓冲，旧R16.1导出字节一致，1920×1080 PNG保真，两份MP4完整解码；六秒成片与取消/恢复/缓存保护源码安装版一致。受测源码哈希封包前硬核对。
完整项目未全量回归；Windows实机、固定FFmpeg、浏览器原生file/localhost、跨文件系统断电/额度压力未验收。沙箱原生导航被策略拒绝；浏览器通过隔离文档和测试适配器对接真实HTTP服务，不冒充Windows端到端。服务器做外层完整性，恢复语义由既有浏览器读者完成。初始失败和修复日志原样保留。下载实际在用户电脑落盘仍需用户选回确认。

## 档案索引
- TASK_本机续作_2026-09-12_12-30-33.md：本机续作接口、边界和重现入口。
- REPORTS/local-continuation/VALIDATION.md 与 PROJECT_REVIEW.md。
- tools/delivery/LOCAL_CONTINUE.py、CONTINUE_CLIENT.js：可选服务与界面。
- tools/workbench.desk.js：来源明确的恢复入口。
- tools/user_ready/build_continue_delivery.py：实际校验后排他发布。
- tools/user_ready/rehearsal_continue.py：测试专用浏览器/HTTP适配与真实媒体演练。
- 上一有效快照 PROJECT_STATE_2026-09-12_10-59-13.md。

## 假设
假设可选本机Python入口适合部分用户设备；本轮没有用户设备或Windows实机，不能确认安装条件。大素材完整ZIP频繁快照存在性能代价，未实现差分存储，不能宣称最大额度性能已经通过。

## 变更日志
2026-09-12_12-30-33：新增可选本机续作，显式恢复来源隔离备份状态、故障保旧、实际源码/安装版演练与排他交付。旧状态原样保留。
