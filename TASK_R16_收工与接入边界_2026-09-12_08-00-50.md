# R16 任务快照：收工与接入边界

定稿：2026-09-12_08-00-50 UTC。后续新建任务，不修改本档。

## 不得丢失的选择
Desk/v1是外壳，不给Studio/v1加入未知键；固定三个stored成员DESK.json/STUDIO.zip/MANIFEST.json，内层可直接提供给原R13至R15。哈希和严格ZIP目录检查皆需保留。Python验证流式读取内层于私有临时目录，不联网；只读desk-verify，desk-extract-studio排他创建，不覆盖旧备份。
DESK保存的是已加载但未应用的ExternalEdit与CreativeTemplate和三个草稿输入，不保存预览/确认/撤回；旧批准只是旧历史。恢复先核验内层媒体、预检所有DOM字段、再明确确认。一旦现有工作或缓冲改变，旧预览不得复用。
统一入口只分流：协议身份支持Desk/Studio/Workspace/Repair/Director、Request/ProjectImport/Catalog/Template/ExternalEdit/Review；TXT先有本次EDIT；媒体先选用途。不能因此接收任意执行代码、远程URL或宣称任意视频/工程可恢复。
新改稿/模板无效时旧缓冲保持，但旧预览与确认清除。原R14/R15测试曾要求清空，已明确更改此语义并保留原失败，未来不要“修回”丢稿行为。
收工包元数据最大2MiB。超限单独保存，不增加隐式压缩/裁剪。原图视频不变，原能力档不变，名单过期仍按原机制提示。

## 验证与证据
原R15页面和Python验证器实际读内层并重导出字节一致；源/独立安装版收工ZIP与六秒成片一致。69项新增测试及既有选定回归，精确终态读R16_VALIDATION/EVIDENCE而非本档推断。安装包重复构建允许ZIP时间戳不同，但所有运行时文件必须与受测独立安装版逐项一致。
没有Windows原生双击、完整回归或商业生成保证。下载哈希通过不是客户端保存完成，不得再次凭文件名发送不存在的链接。

## 后续限制
需要全部主影片工程请用原pack/unpack；Desk不是整工程包。任意NLE语义回程、超大文件流式浏览器封包、自动持久化与多人协作不在本轮范围。新增这些能力前先保持当前旧格式往返与用户确认边界。
