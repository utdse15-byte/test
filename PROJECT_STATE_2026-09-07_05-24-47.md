# PROJECT_STATE

快照：2026-09-07T05:24:47.647330+00:00 UTC。仅最新时间戳有效，旧快照原样保留。

## 核心目标与阶段

用户要求恢复 R3/R4 并继续 R5/R6/R7，最高优先级是交付真实可下载、可恢复的文件。
当前阶段 R7；这是基于已校验 R2 的功能重建/改进候选，不是原 R3/R4 字节恢复，不是 Windows 正式绿灯。
原版 R3/R4 ZIP 未存在于本轮附件中；不可再次声称它们已经恢复或完整继承。

## 有效决定和理由

R2 13a2ab12dac109bece5a2be63254024c11ebc390 及 789 个提交已通过原包验证器、独立 Git 恢复和 1787 文件校验。
分阶段独立封包，后续版本累计继承已提交代码，不靠聊天说明判定版本关系。
模型档仅作离线创作/交接，不自动执行 API，不把营销页当实测能力；过期信息提示重新核验。
每份新 ZIP 先实际生成，再独立解压验 hash、Git 与 wheel；下载 URL 不可由文件名推断。
R6新增定稿包v2必须带实际批准草稿和审片链；旧v1包继续可读，但旧定稿哈希声明不冒充晋升证据。
任务Request/v1规范化哈希保持不变；来源上下文和版本记录放在外层，不自动改写影片工程。
模型工具以文件交接接回原GUI，只读下载镜头；不自动载入或覆盖浏览器草稿。
本地取消不再作为一般失败触发回退；本地清理未确认时报告失败，绝不宣称取消成功。

## 硬约束与背景

Windows-first、local-first、单用户；文本/YAML为真相，媒体只增不改。
保留严格零费用模式，不调用商用生成服务、不使用用户凭据、不自动选片/审批/Picture Lock。
不新增内部LLM、MCP服务、React/Electron或远端UI，不将报告当生产输入。
本环境为 Linux / Python3.13.5 / FFmpeg7.1.5，不是固定6.1.1的Windows验收。
R2 的 PROJECT_STATE、TASK、源 ZIP 和历史绿灯 REPORTS/LAST_GREEN.yaml 保持原样。

## 本阶段功能记录

- 本地进程取消/清理失败区分
- 截图先验证后原子替换
- 取消不启动回退
- 已完成候选在生成、单镜重做、批量重做中保留
- 按型号和模式的离线能力档
- 任务规划与约束校验
- 人工批准绑定请求哈希
- 不可变外部模型交接包
- 离线浏览器工作台
- 本地文件哈希与任务文件导出
- 项目镜头导入
- 能力目录过期与互斥提示
- 内容哈希绑定的候选盲审
- 草稿批准和定稿晋升
- 候选文件不覆盖
- 离线对比与审片报告
- 原影片界面的模型入口与只读镜头导出
- 累积版本识别与下载安全核验
- 隔离安装/启动入口
- 诊断与恢复工具
- 跨阶段继承、wheel和Git恢复核验
- 命令和新协议的显式兼容登记

## 任务档案索引

- TASK_Recovery_Delivery_2026-09-07_04-05-00.md：缺失历史、重建约束及交付方法。
- REPORTS/rebuilt-delivery/：本轮分阶段报告；只有实际保存的测试结果才计入验收。
- TASK_Release_Gates_2026-09-07_05-24-47.md：尚未关闭的发布验收、精确继承和下一窗口不可重复误判事项。
- DELIVERY_VERSION.json：机器可读阶段与累计继承表；提交身份见包内 PACKAGE.json。
- 上一状态：PROJECT_STATE_2026-09-07_04-48-08.md

## 未决事项与假设

Windows实机安装/退出/进程树取消/升级回滚尚未验证；不能用Linux模拟代替。
当前 Chromium CLI 截图路径仍可超时，原样记载；PNG完整性和取消用例不等于截图渲染成功。
Hypothesis/xdist/build/ruff缺失且安装因网络DNS失败；不能宣称全量收集或静态工具全绿。
R7核心分组验收通过不能冒称同进程完整回归通过；一次120秒组合截止及固定R6扩大回归结果原样保留。
缺依赖的真实离线安装按预期拒绝并清理新环境；只有宿主依赖支撑下的隔离安装验证，没有全新Windows离线安装成功。
假设：新的型号档能减少交接错误，但真实生成画质、速度、价格尚未通过用户账户验证。

## 变更日志

2026-09-07T05:24:47.647330+00:00：阶段推进到 R7，旧状态未修改，保留实际交付优先约束。
