# PROJECT_STATE

快照：2026-09-12_04-45-30 UTC。仅最新时间戳有效，旧快照和任务原样保留。

## 核心目标与阶段
用户经常用外部工具，要求检查所有部分的导入导出与灵活性并直接实现。仍只选顶尖模型、不为价格或速度降档；可实际保存到本地的下载文件是必要交付。
当前R15，基于真实R14 Git `f0df46d95e2113ba5d89bef30df518455672de50`，原ZIP SHA-256 `179e736e226c737865dacd563d691119d61fea982cc7b8759779271aa3a1aa0c`。R3/R4为功能重建而非原包字节复原。具体最终提交、文件与发布校验以PACKAGE/EVIDENCE/独立receipt为准，不从状态或聊天推断客户端已落盘。

## 有效决定与理由
R15外部编辑与不可变备份分离。EDIT.json只编辑values，独立TXT按基线ID绑定字段，可按区域带原字节媒体；不改变Studio/v1、Workspace/Repair/Director旧格式。新Studio结果已由原R14页面和Python验证器实测恢复。
三方比较原文/当前/外部；冲突默认保留本地，显式选取；上下文镜头/媒体/锚点改变拒绝强套。应用前检查每个浏览器字段能原样容纳，不以静默清空处理非法数字。UTF-8 BOM可读，TXT CRLF/CR明确转LF，其他空白保留。
五区域25固定文字字段加最多16锚点目标。未提交审片说明可编辑，历史批准/评分/型号/素材不可借此改写。媒体保留原字节、可识别扩展名和用途，未知扩展名.bin，不传字体或执行脚本。
源/安装版真实图视频往返一致。镜头创作变更回draft并清除draftHash/sourceContext；只改导演/说明不降级未变镜头。应用均清当前确认且启用质量优先；历史只作历史。
未应用改稿缓冲单独另存JSON，不纳入旧总备份；应用后的文字才被总备份覆盖。新预览遇继续编辑失效。单步撤回只在本页面且没有后续修改时有效，不是持久版本历史。
CLI external-create/kit/texts/preview/apply只读或排他新文件；apply要求明确字段、confirm、preview_sha256，源包变化拒绝发布。
主工程已存在pack/unpack、参考/模板包、字幕出口和有基线的受限OTIO/FCPXML/剪映差异骨架回程。任意外来FCPXML/EDL/OpenClap为plan-only。project_bridge仍只读，无离线改稿自动写回主影片。必须保留此分界，不能以“所有部分能导入”宣传。
本轮不研究新排名，原十二能力档/质量名单、媒体/供应商/构建代码保持。R14组合与模板、R13总备份、R2保存与R3取消保护继承。

## 长期约束和偏好
Windows-first、local-first、单用户；文本真相，媒体只增不改。顶尖外部创作工具优先，代理零费用、不用用户凭据。本地占位不是商业生成。
不引入内部LLM/MCP/React/Electron/远端UI，不自动上传、付费、选型、选片或锁片。云端计费/任务状态不可由本地取消推断。旧ZIP、PROJECT_STATE/TASK、LAST_GREEN保持，不承诺永久托管或已存到用户设备。
原上限128MiB单文件、512MiB去重素材、16锚点。总备份仅覆盖三个离线区，不包括主影片工程、外部NLE工程、未绑定文件、未应用缓冲或撤回点。

## 实测索引
8个互不重叠选定回归组833节点：832通过1跳过，75项新增全部通过。833节点单次组合调用未完成，不能称同进程全绿。全7082节点仅收集，3个Hypothesis模块缺失，未全量执行；Ruff未运行。原始中断/失败与脚本更正均保留。
10组真实合成演练涵盖源/安装版总备份、交换、组合模板、取消出片，以及主工程打包/有限回程与老R14读新结果。Windows实机、固定FFmpeg6.1.1、商业执行仍未验收；Linux隔离浏览器不代表原生双击。全部精确范围读报告和EVIDENCE。
- `TASK_R15_外部往返边界_2026-09-12_04-45-30.md`：协议上下文、冲突、并发、CLI和后续边界。
- `REPORTS/continuation/R15_PROJECT_REVIEW.md`、`R15_INTERCHANGE_MATRIX.md`、`R15_VALIDATION.md`。
- `tools/delivery/QUICKSTART_ZH.md`：用户入口和外部往返流程。
- `PROJECT_STATE_2026-09-12_02-35-17.md`及旧索引：历史决定。
- PACKAGE、SHA256SUMS、独立receipt：最终版本/恢复身份；不能替代测试。

## 假设与未决
假设：可编辑材料与三方比较减少重复粘贴和错稿；没有真实用户长期亲测，不给百分比收益。
不是任意第三方格式转换器。主影片故事/YAML的一站式语义导回、任意NLE完整还原、EXR/PSD等母版编辑、多项目标签页、大媒体分块、自动同步和真实商业执行未在本轮实现。

## 变更日志
2026-09-12_04-45-30 UTC：记录R15外部工具往返与全项目进出审计；旧快照保留。
