# PROJECT_STATE

快照：2026-09-13_17-53-56 UTC。仅时间戳最新一份有效，旧快照原样保留。

## 核心目标与当前阶段
个人本地影片制作，Windows 11 优先；用户将长期使用本地 IDE AI，同时要求界面舒展、直观、成果真实可下载。维护候选 0.2.0+r16.10，从实际 r16.9（4537740）和 Fable 评审之后推进。完整读取的上一状态为 PROJECT_STATE_2026-09-13_17-31-26.md。原 MANJU_IDE_FULL.zip SHA-256 a14b1ee2abc954325f3f9ee0abf5a886a612f80665dd37e37f87980080f51e95，不修改。
R3/R4 为功能重建，早期丢失的 R17/长篇尝试没有被恢复。现有故事层从 r16.8 开始，不能冒充早期缺失包。实际交付、最终提交和下载成功仅以 PACKAGE / DOWNLOAD_AUDIT 为准，不由本状态预先盖章。

## 本轮决定与范围
先实现只读的故事差异，不等待事务系统，不新增审批面板或不可用采纳按钮。由两份已校验 Story/v1 派生独立 manju.story-diff/v1，作品格式不迁移。稳定ID识别场景/依据/简报，位置只表示实际排列；同名不同ID是增删，不同项目不按碰巧相同的ID合并。正文按整字段完整保留空白、Unicode和换行，引用/范围/旧版本变化也有明细。新增插入导致的位置后移不冒充重写；不能单凭位置判断哪一场由谁移动。
ide-preview 增加 story_diff；可选 --html 新建离线无脚本只读报告，不覆盖旧报告。旧命令和必填参数不变，preview哈希会因内容增加而变化，跨升级必须重新preview。不添加写入命令，也不自动改旧简报/批准/素材。
原故事JSON导入、收工包恢复预览直接展示当前网页与待恢复文件差异。它不是原导出基线的AI增量，更不是故事三方合并；其中可能包含用户后来写的文字。继续编辑使预览失效并移除旧对照；取消/展开对照不修改作品与备份指纹。坏新故事JSON会清空旧预览而保留当前正文。不同作品只提示替换，不制造同ID匹配。
默认能看到改前与改后、结局字段/场序是否字面变化，其他未改字段按需展开。没有把结局字段未改包装为剧情语义未变；报告不证明实际作者或模型身份，可能含作者计划与私密历史。核验完成已在预览中显示，不再重复弹Toast遮住对照；真正错误仍显示。
只读差异不是选择性接受。结构依赖整组应用、自动冲突合并、常驻AI工作间和长篇去重均仍未实现。当前整包恢复仍整体替换，后来网页新稿先保存。不要因本轮而删除分支或历史实际内容。

## 已有创作与接管边界
AGENTS.md统一入口，CLAUDE.md引用；先 tools/ai_bootstrap.py --json 后读最新状态、docs/ai/OPERATIONS.md，按需Skill，不把历史IDE_HANDOFF当当前任务。源码APP/source是快照无.git；APP/repository.bundle用于克隆到新目录维护。主影片工程以实际project.yaml/series.yaml为对象，不在源码目录存作品；未保存网页不是IDE文件。
IDE副本只编辑STORY.json及EDIT.json的values。基线和媒体只读，已有故事身份/链接/旧简报/旧观察不可偷偷改写。返回只生成新候选文件，需明确恢复。文字仍有旧EDIT三方比较，故事全本另行审阅。清单不是签名；AGENTS不是权限沙箱，不保证任何客户端/AI永久理解或遵守。
故事依据分显式知情，人物视角不自动继承其他场景；自由文本不能自动防剧透。简报绑定实际内容和真实场序，不改写历史；未引用资料不牵连无关版本。既有series Bible只读导入不是持续同步。历史哈希不是素材文件，重要媒体需实际绑定或另存备份。
无故事Desk/v1、有故事Desk/v2，Studio/v1继续不变；收工包括故事、三工作区、待处理外部改稿/模板，不含主影片工程、外部剪辑工程和未绑定文件。本轮对照报告/临时预览不是收工内容。旧r16.9可读新收工包。

## 长期硬约束与偏好
只用顶尖质量，不为省钱或提速降档；12视频能力档、quality名单和image_routes本轮原字节保持，不研究新排名，不运行商业生成、上传或付款。生成、候选回收、批准、选片、锁片分开，不伪造人工观测。原媒体只增不覆盖、文本是真相、派生报告不作构建输入。LAST_GREEN不推进。不分发字体、凭据、完整第三方书籍/Skill。
普通HTML免安装；实际CLI需Python3.11+与依赖，媒体处理另需FFmpeg。可选本机续作需主动开启，独立备份提醒保留；按本窗序号保留3份恢复点，新点成功才清本窗旧点。本机恢复不等于第二份备份。
单媒体128MiB、去重总512MiB、16导演锚点；故事规范化UTF-8 JSON 1MiB，512依据/512场/128简报。Fable探针60场×800汉字、前情0/5/20分别可保存60/58/26份，不是用户真实上限，也没解决容量问题。未来去重需保留实际旧内容，不能只留哈希或默认删未采纳版。

## 实测与遗留
本轮已有21项基线通过；动作反转缺少原文/新文的红测试复现后修复。执行范围与最终XML、退出码、浏览器演练及封包记录在EVIDENCE；源码/安装版/最终包复验分别记录，重复节点不累加。早期两次测试脚本定位错误（未导航故事、调用闭包函数）保留原日志，不把测试程序错误宣称产品缺陷；另有工具超时中断，不冒充完成。
没有全项目回归、Windows实机双击/完整依赖安装/退出升级回滚/固定FFmpeg/最大容量/全部辅助技术验收。没有商业IDE客户端测试、商业图视频或整片渲染。原始HTML在隔离浏览器执行，不冒充Windows原生file导航。下载文件真实存在和用户电脑实际落盘分别判断。

## 档案索引
- TASK_可读故事差异_2026-09-13_17-53-56.md：首期只读差异的范围、设计边界、下一步验收。
- src/manju/authoring/story_diff.py、ide.py；tools/workbench.storydiff.js/.css；既有导入/恢复预览。
- tests/test_story_diff.py、test_story_diff_workbench.py；原r16.9选择范围继续回归。
- REPORTS/story_changes/；tools/user_ready/rehearsal_story_diff.py 与 build_changes_delivery.py。
- TASK_Fable深谈_2026-09-13_17-31-26.md 保留讨论及未实现建议；上轮IDE任务与既有制作档案继续有效。

## 假设
用户常在网页与IDE往返但实际并发频率未知；本轮不以推测频率建常驻同步服务。用户真实长篇规模未知，容量仍按已有事实说明。具体IDE品牌、用户电脑环境和最终落盘状态未确认。

## 变更日志
2026-09-13_17-53-56：从Fable评审的已复现缺口进入可读故事差异，实现只读预览与无脚本报告，不新增批准或迁移格式，实际交付按最终审计。
