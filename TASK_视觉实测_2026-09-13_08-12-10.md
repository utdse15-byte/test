# 视觉实测与维护交付

定稿时间 2026-09-13_08-12-10。范围、原因和未关闭事项见同时间全局状态及REPORTS/inspection。不得把本轮当作新模型接入或Windows正式全绿。

关键技术：不要重新引入dialog close异步竞态；不要把验证器复制成另一套UI规则；不要把底部状态改回无限推迟的防抖；不要让普通typing触发遮挡弹窗。查看器只借用director的原图URL；workspace替换即退出，不创建新批准或导出缓存。

旧安全用例不能继续假设DOM零img，应验证导入没有增加节点/改变现有src及执行脚本。对应原始失败保存。不通过延时或force-click绕过UI。

选定终态794个节点中793通过、1跳过；源/安装均有3类媒体实测。真正外层发布须跑build_inspection_delivery.py，再从实际最终ZIP重新安装、运行rehearsal_inspection.py，再跑rehearsal_downloads.py --prefix MANJU_FLOW。未完成不得给猜测下载链接。

后续最高杠杆：真实Windows/真实用户设备验收，以及真实素材色彩/高分屏观感。当前仅Chrome隔离文档、合成素材和已声明规格；不要降低质量约束以追求测试绿灯。
