# 重建选择性接回：任务定稿

全局状态：PROJECT_STATE_2026-09-14_10-44-29.md。本轮唯一基线为用户重传r16.10/Git80ca2e0；上一轮丢失包无可继承代码。维护版本r16.11重新实现，最终文件前缀MANJU_FINAL_R16_11。

## 不要退回的决定
基线/候选来自原IDE快照，不要求AI编另一套操作语言。新增JSON为派生协议，不升级Story/Desk。文字稳定ID+整字段三方比较；结构单组保守，不声称完整依赖闭包。删对象要比较完整原文，不可抹本地新文字；只在组合后全Story合法时一次安装。
候选不能改既有简报/观察/顺序/project_id/link；当前本地历史与link不因文字接回消失。未选字段保持，不自动覆盖冲突、不重新勾选刚取消的余项。一次撤回受现场指纹保护，不恢复批准。
packet是独立待处理文件，未自动写入收工buffer；已应用正文照旧保存。对照文案明确含作者秘密和完整历史。新packet大于2MiB旧元数据界限时必须用自己的严格容量校验，每个story仍1MiB；紧凑导出，不丢原文。异步后到的旧回调不能改变最新预览。禁止为过门改校验值/旧基线。

## 实际验证路线
源与独立wheel均从真实旧收工例子出发，IDE改第二场动作/对白；网页之后改第一场+第二场对白；只有动作能接回。实际点击/撤回/再次采用/保存/选回/新窗口播放/旧r16.10再导出；五份素材和内层STUDIO保持，目标图1920x1080。重复packet标已一致；新输入使预览失效。脚本操作不等于商业IDE模型已认证。
最终候选须一套完整原回归加新测试、源码冻结、source/installed RESULT、独立解压Git与wheel、最终包新安装、浏览器真实下载/五份乱序合成。不可因前一阶段成功直接宣布下载。准确执行数见终态XML和审计。

## 后续边界
未做语义三方合并、精细结构组/关系图、長篇存储去重、跨窗口事务或付费自动生成。Windows完整矩阵和全项目测试未跑。新版本不覆盖旧目录；文档不能承诺用户以后永远不需适配。
