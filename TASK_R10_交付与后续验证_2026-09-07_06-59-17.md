# R10 交付与后续验证

## 不得重新误判的事实
这次从真实R7归档恢复，R8/R9已经独立提交与封包；R10承接两版，不是另开一条未合并分支。
原版R3/R4的丢失ZIP没有找回，累计的R3/R4是基于R2的功能重建。不可声称字节复原。
Omni Flash精确型号早在R7目录中存在，R10只重核验，不新增重复别名。最终默认目录9个档，原8个归一化内容哈希不变，新增Runway Gen-4.5网页作者档。

## 关键取舍
完整工作现场是显式ZIP检查点，实际媒体闭合保存，不靠localStorage；恢复不自动确认或选片。
R9返片规则是本地排错阈值，不是供应商质量标准。Runway超宽720p的实际短边672会触发最低短边提醒，这是规则限制，不可据此断言供应商失败。
R10能力导入先预览影响再确认应用；已导出比较只是当时快照，不具远端撤销、签名或付款含义。
Request/v1不改哈希。新比较报告是独立派生协议。历史交接包以自身能力快照校验，不改为最新目录。

## 已执行的终态验证
EVIDENCE/r10-final-test-summary.json列出42个模块的774个不重复节点：773通过，1个浏览器原生file重载用例因环境跳过；六个分组均取得真实退出码0。
最终完整收集6704个节点，另3个Hypothesis模块收集失败；并未全量执行6704。不要把774个精选节点说成完整回归。
EVIDENCE/r10-closed-loop/与r10-installed-workflow/：真实浏览器四次下载，实际视频完整备份、新上下文恢复再播放，返片检查、草稿晋升和定稿交接核验通过。审批属于合成测试夹具。
EVIDENCE/r10-installed-source-parity.json：独立安装版确实从target目录导入；源和安装HTML一致，252个指定后缀运行时文件一致，六秒合成成片逐字节一致。包级验证器还会覆盖全部运行时成员。
EVIDENCE/r10-chromium-cancel/：真实截图进程取消、旧封面保护、无关观察进程不被误杀。

## 原始红项与环境限制
早期组合调用未得到完整退出收据；保留原始日志，使用独立完整分组复验，不伪装组合全绿。独立GUI退出诊断仅主线程存活，但这不足以证明先前外层超时根因。
发现并修复实际长哈希窄屏溢出，重现和修复后测试都在EVIDENCE。其他早期脚本错误和过时档位计数断言同样留证。
Windows安装/退出/更新回滚及进程树必须实机测；FFmpeg固定6.1.1未测。Linux和isolated_document不能冒充Windows file://双击验收。
Ruff/xdist/Hypothesis等开发依赖未装成；商业生成0调用；付费、模型质量、速度与真实远端取消无实测。

## 文件恢复入口
优先完整累计包MANJU_R10_CUMULATIVE.zip，免安装工具为MANJU_R10_OFFLINE_TOOLS.zip。
包级身份查PACKAGE.json、DELIVERY_VERSION.json、repository.bundle和封包receipt；不要从聊天版本号推断代码是否存在。
完整ZIP必须在实际生成后经VERIFY_PACKAGE.py --git新目录解压验证通过才给链接。客户端是否点击保存不能由服务器哈希证明。
