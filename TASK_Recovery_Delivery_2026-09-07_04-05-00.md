# TASK: Recovery and real delivery

## 不能再误判的历史

本轮读到的真实附件包括完整R1/R2 ZIP、R2报告、R4研究/失败报告、两份中间代码节选。
此前聊天中的 R3/R4 成功交付声明不构成文件存在证据。R4验证报告实际仅记录候选门失败。
原版R3/R4无法字节恢复；基于完整R2重建已描述功能，并按新官方证据纠正不实能力。
R2原包实际验证：1787文件、1411跟踪文件、Git恢复13a2ab1成功，日志存 evidence/baseline-verify.log。

## R3技术决定

纯本地 caption_card / kenburns 在自己的线程绑定取消范围，不给所有云提供商强套本地取消范围。
MediaCanceled携带已注册候选；MediaCleanupError表示无法确认清理，不能回退或谎报取消成功。
POSIX只向自建session发组信号；Windows先taskkill树再处理直接子进程，失败返回不确定性。
输出捕获采用临时文件+轮询大小限制，允许一个轮询间隔的超额，不是硬磁盘配额。
截图使用隔离profile、独立临时PNG、CRC/结构/尺寸校验，再原子替换；错误不覆盖旧封面。
程序不能保证终止主动脱离session的恶意子进程；Windows尚未实机验收。

## 测试边界

测试中的显式选片只用于本地合成媒体夹具。不得把可渲染字幕卡报告为已满足Picture Lock。
完整回归、专项回归、局部复验分开记，不累加重叠覆盖数，不隐藏第一次失败。
工具调用超时曾中断一次测试，未产出完整JUnit的运行不计通过；其子进程已按已知所有者清理。
当前Chromium直接截图仍会超时；data URI实验同样失败，因此未盲目替换生产实现。
字体文件不分发，秘密配置不分发。原ZIP不修改；只有真实存在且校验成功的包才给链接。

## 跨版本交付

R3至R7以独立Git提交/标签和累计源码交付。每包有源码、wheel、全历史bundle、补丁、核验工具。
报告的哈希是完整性校验不是密码学签名；用户点击是否成功仍以客户端实际下载为准。
新电脑需要Python及媒体依赖。离线网页工具与完整影片引擎是两条明确区分的入口。
