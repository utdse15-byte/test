# R14 任务快照：组合与模板

以 `PROJECT_STATE_2026-09-12_02-35-17.md` 为最新入口。本任务定稿后不修改。

## 后续不能做错的决定

组合输出仍为Studio/v1，不要增加强制迁移。shot含镜头/审片/辅助输入，catalog独立；返工上下文、导演实际图像和不选的区域必须保留。
模板预览和应用是纯文字事务，不依赖完整媒体绑定；不要重新强迫buildStudio成功才能填模板。完整备份仍拒绝缺实物。
改动镜头字段退回draft并清除draftHash/sourceContext，不修改历史review。只改director不降级未变镜头。应用清除当前确认并开启quality-only。
预览after哈希指STUDIO.json，不是ZIP。清单引用顺序和文件名必须匹配原浏览器恢复导出，不能任意按hash排序后声称身份相同。
CLI二次打开来源时也需有界复制并核对hash，不可退回无限解压；输出排他创建。新编辑使预览或撤回失效，不能覆盖后来文字。
临时来源、模板草稿、预览和单步撤回不在总备份中；命名副本没有后台分支树。模板中的文字不自动脱敏。
原始红项与终态须读R14_VALIDATION和EVIDENCE；收集数不是执行数。新wheel/网页必须和受测文件逐项一致再封包。

## 未完成范围
完整影片工程备份、Windows实机、可执行插件、新商业API、大文件分块和跨设备同步均未实现。本轮未更新模型榜单。
