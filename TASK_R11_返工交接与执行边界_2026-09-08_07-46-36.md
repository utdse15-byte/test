# R11 任务快照

日期：2026-09-08T07:46:36.866984+00:00。此任务档定稿后不修改。

R10真实基线为be5923a58005dd85e18f593134f853d886c69a2b。R11为累计工作，不是再次恢复丢失原包。最终提交以PACKAGE.json为准，避免自引用提交哈希。

## 不可混淆的决定
- 新协议manju.repair-plan/v1、repair-manifest/v1、repair-verification/v1，完整原片四条目stored ZIP；拒绝额外条目、前缀/尾随数据、重复键、深JSON、重哈希但不符计划的说明、源字节不符、非规范默认补全与类型转换。
- Source视频元数据创建时测量；verify不会独立重探，报告明确false。哈希仅是内容完整性，不证明来源身份、版权、质量或被编辑范围外不变。
- request/v1只作为原上下文。返工需实际原片、范围和明示请求人确认；不是审批或付款授权。恢复清除确认，不动上方任务/审片；必须单独备份返工ZIP。
- 显式宽高才生成return-preflight/v2。旧v1默认最低短边保持兼容，未自动更改；参数不进入旧任务/现场备份。浏览器和Python对齐，精确像素不推断生成供应商。
- seedance-2-5-las型号dreamina-seedance-2-5-260628按LAS输出4..30s/480p/720p，旧9档不改。视频输入文档有疑似错行，不补上猜测时长。32素材本地请求上限未扩大。

## 实测与已知限制
参考R11_VALIDATION和EVIDENCE/FINAL_TEST_STATUS，不累加重复用例。终态461节点460pass/1skip，新增91全pass；6795仅收集另3缺依赖错误。源码与独立wheel都跑真实媒体返工导出、跨窗口重新打开、原片保护、v2核对/完整解码；R3取消恢复成片仍有效。

原始CSP谓词失败、原生file导航错误与外层执行超时日志保留。修正测试轮询表达式为函数，页面CSP没有放宽。实际file://和Windows双击仍未验收。

## 后续工作不可跳过
优先真实Windows用户路径、文件下载/保存、旧现场和独立返工ZIP恢复的验收；然后才扩展实际模型执行。
引入执行前必须明确零费用约束与用户授权、对应型号/入口规格、任务ID/超时/取消/下载有效期，不能把作者档当可执行适配。
LTX尚未下载权重或取得许可/硬件验证；2.5Fast本地不应继承2.3Fast的Retake/Extend。Luma关键帧口径仍待确定入口。
R11报告的所有改进不证明AI真的完成局部修复，也不证明自动省钱。
