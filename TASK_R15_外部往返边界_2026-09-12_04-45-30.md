# R15 外部往返任务快照

定稿：2026-09-12_04-45-30 UTC。此任务记录不再改写。

## 保持住的架构
外部改稿是新manju.external-edit/v1纯数据，备份仍Studio/v1。三方preview基于values+contexts，散列检测意外混用不证明作者身份。不要把旧SHA256表改成让用户随意编辑备份的方式。

路径键如shot/prompt、review/notes、repair/repair-start、director/director-change、director/anchors/A01/target、settings/return-width。上下文分别绑定镜头标识/素材/source_plan、候选审片session与pending、返工request/source、导演source/shot、锚点id/time/frame/guide。上下文变化的字段不得“强制冲突应用”。

TXT文件名包含base_sha256前16位和字段序号。仅同一EDIT接受；实际文本支持BOM和CRLF/CR归一。保留所有其他空白和明确空串；浏览器不可表达值预检整批拒绝。JS与Python须同时改并加一致性测试。

## 不能退步的行为
只修改勾选字段；外部没改的字段保留本地。应用后镜头意图变化清draftHash/sourceContext，审片历史不改，所有当前确认清除。不要从导入文本自动批准/选择/绑定媒体。

预览/应用异步读取必须重新核对studioGuard、serial、确认和勾选集合。纯JSON导出不要求媒体，带媒体导出必须引用实物、限制大小且逐项哈希，源文件变化不得发布。CLI外部apply必须预览哈希+显式字段+confirm，新文件排他发布。

外部缓冲和撤回点不是Studio备份的一部分。已有“另存未应用的外部改稿”保留TXT覆盖后的values，导入失败释放旧缓冲状态，不能还显示旧已加载成功。

## 重复排查免疫
crypto.subtle在隔离浏览器可不存在。竞态测试用显式可释放的摘要屏障，不靠延时和WebCrypto存在假设。旧R14的CSP拒绝对表达式字符串eval，Playwright wait_for_function用函数形式，不削弱CSP。旧apply_roundtrip rows为keyword-only且Python零基，CLI才是一基显示。

运行时受测文件由EVIDENCE/TESTED_RUNTIME_SHA256.json固定。8组833节点单组实际rc0；早期合并调用外层中断，不抹去原日志。真实演练脚本rehearsal_r15.py和rehearsal_r15_project.py随源码。

## 未完成方向
任意NLE/原生软件工程完整读回不受支持；外来FCPXML/EDL/OpenClap计划不执行。project_bridge只读，不是主影片任意文件修改入口。没有商业模型调用、Windows实机认证、跨设备同步、自动保存或放宽大文件限制。
