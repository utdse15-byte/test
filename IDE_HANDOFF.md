# 本地 IDE 接管入口

当前入口为 `AGENTS.md`。先运行 `python tools/ai_bootstrap.py --json`，再阅读它列出的最新项目状态与 `docs/ai/OPERATIONS.md`。

历史的 2026-08-12 零成本任务交接保存在 `docs/history/IDE_HANDOFF_20260812.md`，其中分支、未提交工作和任务授权不能当作当前状态。

浏览器草稿必须先保存收工包，才能通过 `manju models ide-open` 交给 IDE AI。完整包包含源码；轻量上手包不包含可供开发的完整仓库。任一模型的自动识别与理解程度仍取决于所用工具、版本、授权和上下文。
