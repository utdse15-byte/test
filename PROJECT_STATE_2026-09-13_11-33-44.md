# PROJECT_STATE

快照：2026-09-13_11-33-44 UTC。仅时间戳最新一份有效；旧快照保留。

## 核心目标与实际阶段
用户要把项目下载到本地IDE，长期让AI协助真实制作；同时明确反馈界面紧凑、不直观。当前维护候选0.2.0+r16.9，基于真实r16.8 Git 7f7d4d7848b0a574356dc182d648805e1da51d0f（813提交），原完整包SHA-256 dddea9aa3ce0136cf572095b188721592274ac03af0f7d1fa240e68db81b6a91。已完整阅读上一有效状态2026-09-13_10-52-39及实际代码。最后提交/ZIP/下载完成状态以PACKAGE和最终下载核验为准，不预先填成功。
R3/R4仍为功能重建。旧R17及先前丢失的长篇尝试没有恢复；本轮不是它们的字节复原。

## 本轮有效决定与理由
统一AGENTS.md入口与CLAUDE.md引用；保留原开发守卫至docs/ai/DEVELOPMENT_GUARDS，按需阅读，避免创作默认变成改软件。原IDE_HANDOFF的2026-08-12任务归档并标记历史；ai.txt第三方分析同样不是当前事实。一次性读全历史不等于理解，入口+当前状态+按需Skill+真实命令输出才是接手路径。
只读tools/ai_bootstrap.py从实际源码判断最新状态、版本一致、依赖缺口和源快照/Git历史区别；不联网、安装或读凭据。ok只代表入口和源版本，非全部运行依赖或客户端认证。完整包APP/source没有.git，完整历史在APP/repository.bundle；维护代码需要从bundle克隆到新目录。完整包附MANJU.code-workspace，任务仅手动只读检查；上手包不含源码/CLI。
新增ide-open/ide-preview/ide-return，以已保存收工ZIP创建普通工作副本。只编辑STORY.json和EDIT.json values，原媒体/基线只读、旧简报及旧观察不可改写，现有故事身份/镜头链接不能通过返回偷偷更换。新故事和正文可以改变；旧简报按原内容机制显示待复核。返回使用本次预览哈希、再读所有输入、排他创建新候选；不覆盖源件，不把未执行步骤写成完成。
网页未保存内容不是IDE文件。返回包是导出基线的完整候选，需要用户明确恢复，不自动合并后来网页新稿；可用旧外部改稿入口三方比较文本，故事全本独立审阅。下轮基于明确接受的新收工包重新ide-open。工作目录NOTES供下次接手但不进入作品，不承载批准。
manifest是完整性约定，不是签名/文件权限沙箱；对有任意写权限的AI不保证行为。未测试商业IDE自身能否按说明执行；代码验证的是工具与文件链，不保证任何模型永久内化全部项目。
界面把故事分出独立“故事与人物”入口；场景写作、参考、版本分三步，元数据折叠；镜头单列，输入16px、按钮/留白增大，保留完整长页与焦点定位。210个原具名控件逐一保留不重复；切换不重建表单/媒体，也不写入创作指纹。

## 已有故事与媒体边界继续有效
可选故事工作本区分作品形态、稳定人物/关系/设定/事实/后果、真实排列场序、显式知情和引用职责。人物视角不自动继承其他场景正文；范围控制不是自然语言防剧透。简报固定实际内容与场序，未引用信息不造成无关失效；内容完全恢复可重新一致，不是从未修改证明。旧简报不自动重写、不重生成。明确链接有过期/提示词/缺参考守卫；解除关联后不再检查原联系。人工复盘与原故事分离。
series共享Bible只读导入，不推断剧本、不反写、不同步未来修改。新层是制作桥接，不是完整关系数据库、长剧自动语义审计或多人编辑系统。
无故事写Desk/v1，有故事写Desk/v2；原Studio/v1和旧r16.8外层兼容。新IDE返回不改协议。收工包含故事、旧版本、3工作区、已加载未应用改稿/模板；不是整个主影片/外部剪辑/未绑定文件备份。历史哈希不是原文件，原媒体需实际绑定或旧备份。故事JSON不含媒体并包含作者私密计划；通常对外发本场简报而非整个故事库。

## 长期偏好与硬约束
个人本地制作，Windows11优先；只用顶尖质量，不因便宜/速度降档。旧作者档不等于最新榜单。12视频能力档、质量名单、精修路线JSON原字节不变，本轮未重新研究排行。生成/上传/付费/审批/选片/锁片明确分离，不自动越过；本轮无商业调用或原工程改写。LAST_GREEN不推进。不分发字体、完整第三方书籍/Skill或凭据。
普通HTML无需Python；CLI需Python3.11+和项目依赖，媒体处理需FFmpeg。可选续作本机标准库/固定环回路由/令牌，须主动开启，独立备份提醒保留。恢复点按每窗序号保留3份，新点完成才清本窗旧点；本机恢复不是第二副本。
单媒体128MiB、去重总512MiB、16导演锚点；故事规范化JSON1MiB、512依据512场128简报、每场64引用32职责。不是无限库，大项目可分卷保存。附件非永久存储，用户本地选回哈希核验并另存备份。

## 已有实测及遗留
源码与独立安装实际跑通网页保存、真实CLI ide-open/preview/return、人物/动作与镜头修改、旧简报待复核、五份媒体原字节、高像素1920×1080 PNG、恢复播放及原r16.8完整外层再导出。两版浏览器最终收工ZIP字节一致。编辑由脚本模拟，不是商业IDE或模型生成。
另实际新建三镜头文本工程并执行status/events/production status/check，15真相文件不变；没有把骨架当剧情成片。本轮没有整片重渲染、全项目回归、Windows双击/依赖安装/升级回滚/固定FFmpeg/最大规模/辅助技术全面验收。浏览器为原HTML隔离文档，非原生file导航。终态回归与最终ZIP再安装/分段下载只认EVIDENCE及最后审计。
早期演练两次失败为测具错误：误用了EDIT键workspace.draft.prompt（实际shot/prompt），以及字符串wait_for_function触发CSP。改为实际schema键与函数谓词；没有放宽CSP或修改产品迎合测试。原日志保留。

## 任务索引
- TASK_IDE接管与舒展写作_2026-09-13_11-33-44.md：AI接手对象、不可自动合并、UI源文件与验收边界。
- AGENTS.md、docs/ai/OPERATIONS.md、tools/ai_bootstrap.py。
- src/manju/authoring/ide.py、3个CLI、CONTRACTS登记；tools/workbench.comfort.*和story/experience视图。
- tests/test_ide_workspace.py、test_ide_onboarding.py、test_comfort_workbench.py、test_ide_delivery.py。
- REPORTS/ide_ux/README.md、PROJECT_REVIEW.md、VALIDATION.md。
- tools/user_ready/rehearsal_ide.py、build_ide_delivery.py；发布收据与原始日志。
- TASK_故事制作闭环_2026-09-13_10-52-39.md及此前恢复/研究档案仍有效，仅本轮明确变化覆盖。

## 假设
用户优先要用IDE的AI制作作品而非继续扩建软件，具体IDE品牌和现有本机依赖尚未确认。提供通用文件入口，不自动设置插件、权限或账号。用户是否在网页与IDE同时编稿未确认，因此保留明确保存/返回边界。

## 变更日志
2026-09-13_11-33-44：从r16.8补齐标准AI接手、可执行冷启动与IDE文件回程，拆分故事写作区域，原协议与质量档保持。旧状态不改。
