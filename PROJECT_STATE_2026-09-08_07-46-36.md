# PROJECT_STATE

快照：2026-09-08T07:46:36.866984+00:00 UTC。仅最新时间戳有效，旧版保留。

## 核心目标与阶段
用户要求深搜X的最新相关消息，并将可取之处直接落实到可下载项目。
当前阶段R11：以真实R10 be5923a58005dd85e18f593134f853d886c69a2b为基线完成代码与实测。交付是否成立以最终ZIP和独立收据为准，不依赖聊天声明。
原R3/R4仍为功能重建，原丢失ZIP没有找回。旧R2至R10归档不修改。

## 有效决定与理由
局部返工协议独立于Request/v1与工作现场/v1，保留原哈希和旧包恢复路径。新增区必须另存返工ZIP，页面与使用说明明确提醒，不能声称旧完整现场包含返工区。
返工ZIP携带完整原片、时间范围、保留/修改要求和原任务上下文，不剪切、上传、调用模型、自动拼接或批准。原任务引用的其他素材未自动打包。毫秒范围是半开区间，不是帧精确编辑保证。
默认返回报告v1不修改。显式像素宽高使用独立v2，避免将Gen-4.5的1584×672按最低短边720误判；按钮只提供明确选择的参考，不推断型号来源。
新增Seedance2.5 BytePlus LAS具体入口作者档，目录共10档，旧9档内容不变。没有商业执行能力；文档不清楚的限制不猜测。
X公开索引仅做线索，原帖抓取失败和日期混杂已记录；官方文档支撑进代码的能力。Luma关键帧口径未厘清不建立确定能力表；LTX本地推理未执行，不复制云端回退。
完整包和安装版共用HTML，已独立安装与实测，不维护第二套页面。

## 长期硬约束
Windows-first、local-first、单用户；文本是真相、媒体只增不改；严格零费用，不用用户凭据。
不引入内部LLM、新MCP服务、React/Electron或远端UI。派生报告不是生产输入。
保留R2保存安全、R3本地取消/候选保留和R4-R10人工批准边界；本地取消不等于云端停止计费；LAST_GREEN不推进。
旧PROJECT_STATE、TASK、ZIP不可改写；输出必须真实文件与独立恢复核验通过。

## 本轮实测事实
终态461不重复节点460通过/1跳过，91新增测试全过；完整6795仅收集，3模块缺Hypothesis，非完整回归通过。
源码/独立wheel完成返工ZIP下载、新窗口恢复播放、实际像素核对与视频全帧解码；256个运行时文件一致。两版返工ZIP和六秒本地成片逐字节一致。
真实截图取消及旧封面保护本轮重新演练通过，仍非Windows实机验收。
CSP测试字符串轮询问题已改为函数谓词，未放宽页面安全策略；原始红项日志保留。

## 索引
- REPORTS/continuation/R11_X_RESEARCH.md：X线索、官方来源、采用与未采用范围。
- REPORTS/continuation/R11_VALIDATION.md：精确测试分组、实际演练和遗留限制。
- TASK_R11_返工交接与执行边界_2026-09-08_07-46-36.md：后续不可误判的协议与执行边界。
- tools/delivery/QUICKSTART_ZH.md：R11使用入口。
- 前状态：PROJECT_STATE_2026-09-07_06-59-17.md。
- DELIVERY_VERSION.json和最终包根PACKAGE.json：版本及提交身份。

## 未决与假设
假设：明确时间范围和原片身份有助于减少返工交接错误；没有用户真实制作或费用节省数据。
返工区尚未合入全工作现场备份，当前明确采用独立ZIP；不要悄悄改旧格式。
默认v1仍是历史最低短边规则；正确像素核对需显式v2，不要声称旧规则已自动变成供应商规范。
Windows安装/双击/退出/升级回滚、固定FFmpeg6.1.1、完整回归、Hypothesis和Ruff尚未验收。
浏览器为原HTML的isolated_document；原生file导航被环境阻止，不能声称Windows双击通过。
所有商业生成调用为0，未下载LTX权重或运行GPU推理。

## 变更日志
2026-09-08T07:46:36.866984+00:00：R11研究、局部返工、逐入口作者档和明确像素检查完成代码及专项实测；下载发布以独立封包收据为准。
