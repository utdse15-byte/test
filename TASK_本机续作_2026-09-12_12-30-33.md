# 任务快照：本机续作维护

定稿快照 2026-09-12_12-30-33 UTC。真实基线 R16.1 / 00ba58ae；新版本0.2.0+r16.2。不是旧R17复原。

## 不能做错的决定
本机恢复点不等于独立备份。只通过 ManjuDesk.preview(blob,{source:"local_recovery"})进入旧有核验/明确确认流程，不用点击后再清标记。source=file保留已有下载文件核验行为。服务发现没有restoreSourceVersion:1的页面会拒绝启动。
新点写完核验后才发布，同窗口只在成功后清理旧点。容量满先暂停。不能为了自动保存看似成功而缩图、丢媒体、覆盖旧点或伪造下载状态。
服务入口LOCAL_CONTINUE与LOCAL_WORKBENCH分离；前者显式开启才写，后者永远只读。直接HTML完全不联网。任何启用自动的偏好只限本页面，不从旧收工包恢复为新授权。

## 结构
恢复根目录IDENTITY/LOCK用于识别与跨进程锁。完成点UUID/POINT.json+DESK.zip；中断点pending-UUID。所有路径拒绝链接；只允许浅层固定文件。每页window id+sequence，避免双窗覆盖。读出先校验SHA，内层语义仍由Desk原读取器完成，不在通用HTTP服务复制整套媒体解析。
HTTP仅127.0.0.1随机端口/随机路径，GET points/points-id和受令牌/Origin保护的POST、DELETE。不能改为公开0.0.0.0、多用户或无令牌API。

## 验证与限制
终态371运行时点+10封包点通过，原失败在EVIDENCE保留。源码/安装版五媒体续作与六秒制作共四组演练一致。Windows原生和真实浏览器导航未验收，测试桥只用于隔离Chromium到真实HTTP；不能进入用户包运行路径。
最后分发文件和测试证据由排他发布脚本封包，文件是否存在和哈希必须现场检查，不从聊天断言推断。

## 后续入口
REPORTS/local-continuation/VALIDATION.md、tools/user_ready/CONTINUE_README.md。
重现：运行tests/test_local_continuation.py和test_continuation_browser.py；需要已有Chromium/Playwright。工具rehearsal_continue.py需要kit、旧示例、legacy-html和全新output目录，实际安装版应从源码目录外运行。
新增功能若改变运行时，重跑终态并更新tested-source-hashes.json；旧日志保留，不能追加一条局部绿灯盖住全组失败。
