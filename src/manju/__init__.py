"""Manju One — a deterministic build system for video.

Truth lives in text files; media is append-only; git is the version engine.
The engine contains no LLM calls: intelligence stays outside the system.
"""

__version__ = "0.1.0"
