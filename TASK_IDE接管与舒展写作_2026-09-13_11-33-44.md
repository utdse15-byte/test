# IDE接管与舒展写作 · 2026-09-13_11-33-44

基线和约束见 PROJECT_STATE_2026-09-13_11-33-44.md。不要再把此候选与丢失R17/长篇尝试混为一谈。

运行时唯一新数据路径src/manju/authoring/ide.py：Desk/Exchange的受控工作副本，非浏览器同步服务。基线+引用原媒体不改，STORY.json/EDIT.json values修改，经preview hash返回新Desk；已有链接、旧简报与旧观察不能改写。新素材仍走原专用入口。保留未应用改稿与模板。

UI源tools/workbench.story.*和experience.*，新增comfort.css/js；生成页由build_workbench生成并两处同步。故事独立导航、场景三步、元数据fold、单镜头单列，不克隆控件。验证默认视图和完整长页，不靠force click去点隐藏控件。

完整包APP/source无.git，恢复历史用APP/repository.bundle。AGENTS/CLAUDE统一，原规约DEV_GUARDS按需读。不要为某家IDE再复制一套互相矛盾的规则或自动开启命令执行。

交付门强制实际test XML/rc、受测代码指纹、源码/独立安装真实CLI与媒体回程一致。最后必须从用户下载包再装wheel复验，然后测试真实5分段浏览器下载。包存在性和用户电脑落盘是两件事；只报实际验证结果。

原r16.8完整外层Desk往返已通过，支持旧页面播放与字节一致再导出。无Windows/商业IDE/全项目/生成API/整片重渲染认证。两次初期测具错误日志保留，最终成功不抹去它们。
