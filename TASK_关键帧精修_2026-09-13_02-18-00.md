# 关键帧精修交接任务快照

## 已实现与代码入口
导演区内逐关键帧任务ZIP，TASK.json全量基线和路线快照、独立TXT、原帧/旧目标PNG。主片不进入外发包。精修返回只绑定未批准目标图，不影响主镜头文字、返工、历史决定。

Python retouch.py与JS共用canonical规则（RFC8785并未声明），使用项目既有canonical/digest。JS选择PNG后完整解码；CLI只检查结构/哈希。单锚点图哈希相同但文件名不同不影响内容用途；返回任务指定文件名必须正确。任务manifest不是来源签名。

全局上下文改变时全部拒绝；逐锚点比较只检查这批返回的位置；已变目标拒绝覆盖；相同图返回幂等。先验证整批文件再变更。预览后任意变化必须重新预览。已接回图经原Director/Studio/Desk保存；未接回材料不加入旧协议，应由用户保留外发ZIP与返回图。

## 版本化策略
route id openai-sunburst-20260908 对应固定API快照与max；同ID记录禁止日后原地改义。normalize_task重新构建固定记录，未来改日期/路线字典时必须保留历史规范验证入口或明确迁移，不能让旧任务悄悄变另一型号。

## 本轮测试
regression.xml/rc：713/712通过/1跳过，39新增含其中。source-retouch与installed-retouch逐字节一致；基线r16.4页面原样回程。具体哈希和安装运行路径见各RESULT.json与installed-parity.json。首轮CSP失败修测试函数表达式，不关闭CSP。重复相同图应幂等，不是冲突。

## 未关闭
没有调用商业图像/视频API，max质量是请求说明而非执行证据；没有独立盲测排名。HDR/EXR/PSD不在此PNG回传支持内；原帧SDR、时间毫秒非帧准确性认证。原生file://与Windows未验收。未做整片生成或六秒渲染本轮复跑。最终下载、最终安装与分段合成以实际发布收据为准，不引用本快照当作交付成功证据。
