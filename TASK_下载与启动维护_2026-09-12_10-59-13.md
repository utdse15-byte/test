# 下载与启动维护任务快照

阶段R16维护0.2.0+r16.1，不是原R17。先读`PROJECT_STATE_2026-09-12_10-59-13.md`。

关键修复：运行时版本与安装元数据一致；VERIFY_PACKAGE使用AST读取原始版本，不执行代码。当前所用LAUNCH在pip后检查__version__；这是实际安装失败根因，不只是标签问题。

传输分段与创作协议完全分离。生成download_parts.py只对本次完整发行ZIP分割，浏览器助手用固定记录的全块哈希授权切片，且验证总包，不能扩成接受任意ZIP的解析器。助手和manifest在最终ZIP之后生成，避免把ZIP自身哈希写进自身造成循环。来源文件保留，输出排他。

LOCAL_WORKBENCH的安全边界为本机、随机端口/路径、精确固定资源、内存快照、无POST和目录服务。不得为方便改成0.0.0.0或SimpleHTTPRequestHandler目录映射。其入口不是自动续作，不保存未完成工作；退出前仍需Desk/工程备份。

测试记录：final-targeted.xml是完整结束的160项。selected-regression.log的扩大组合被工具200秒时限中断，不据点号推算通过数；desk-regression.xml含37通过1错误，错误为未设置隔离传输而遇到file导航策略；最终定向范围另记。probe首次复用导航出错后的浏览器上下文，不是产品缺陷；最终重写成每个文档新页面并通过。网络依赖下载DNS失败保留，不声称新机离线依赖完整。

下一次以最终下载收据核验包，不从文件名推测。旧R16仍可保留作历史，其已知版本安装问题只在新维护版中修好。新维护版与旧作者台文件/模型目录字节不变。
