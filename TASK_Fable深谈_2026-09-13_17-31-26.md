# Fable 深谈任务快照

用户请求仅深度对话，不是新软件发行。唯一生产基线仍为 MANJU_IDE_FULL.zip / r16.9，原件未改。

已完成：五轮真实模型内容交流、两轮完整提交；21项IDE测试和两组独立探针；完整源码未由Fable执行或审计。首轮只有三文档两截图，后续提供源码摘录与实验。

下一窗口先读 REPORT.md 的校正与 SESSION_RECEIPT.json，再看 dialogue 原文；前三轮含已撤回错误，绝不可把原话照单实现。已确认：预览缺故事原新文；哈希不防浏览器后来修改；1MiB可能先于条目数触限。关系仍无原生两端字段。

待做而非完成：只读可读故事对照、选择性整字段/整场接受、结构依赖事务、历史内容去重、权威写入路径。未做新UI、未发版本、未跑商业IDE或Windows。不要把一份讨论包当作应用更新。

全局旧状态随inputs/source保留。应用源码、质量名单、影片原件不变。下载包是纪要与实验资料，不能覆盖APP/source。
