# PROJECT_STATE

快照：2026-09-12_02-35-17 UTC。仅最新时间戳有效，旧快照保持原样。

## 核心目标与阶段
用户要求检查项目灵活性并直接落实个人工作流改进，保持只用顶尖外部模型、质量不降档和真实可下载交付偏好。
当前R14，基于已实际核验的R13 `7284f4716920243fd9e341f3d35ad272975b490c` 与800个历史提交。原R13 ZIP SHA-256为 `1faf5118f1068531301ee8043d37113c75ecbb99b838d6d9aeb3302c687a5d34`。
最终测试与下载身份须读取EVIDENCE、PACKAGE和独立恢复收据，不从状态/聊天推断已发布或已落盘到用户电脑。R3/R4仍是功能重建，不是原包字节复原。

## 有效决定与理由
按shot/repair/director/catalog明确组合既有备份，未选部分保留，目录独立选入。镜头与审片不可分拆混接；返工带原任务上下文，导演带完整底片与图像。不引入任意脚本插件或新的节点框架。
输出继续用Studio/v1，保留R13恢复路径和原单区协议。媒体清单按原恢复/导出顺序与显示文件名构建，使预览后的文档哈希与保存结果对应。原片与高像素图按内容复用，不转码或缩小。
CreativeTemplate/v1只含白名单文字。默认只填空白，明确替换只改列出字段；不自动复制身份/素材/批准，但用户写在文本中的隐私不会自动脱敏。
模板文字编辑与备份分离：未重绑媒体时仍可填模板，完整备份仍拒绝缺文件。镜头意图变化回到draft并清除draftHash和sourceContext，原审片仅保留为历史；只改导演区不降级未改变的镜头。
变更必须预览；异步过程中或预览后编辑/改选区/改模式则失效。应用后只有一个页面内撤回点，新编辑后锁定。来源选择、模板草稿、预览、命名文字和撤回点不纳入旧总备份协议，模板需单独导出。
四个新CLI提供只读差异、排他新包组合、模板提取/核验。组合按已验证大小有界复制并逐项哈希，来源变化拒绝发布，既有输入输出不覆盖。
本轮不重新研究型号排名；十二档和质量名单原样。每次应用仍开启质量优先、清除当前确认，不允许成本/速度降档。模板参数不是供应商能力证据。

## 长期硬约束与确认偏好
Windows-first、local-first、单用户；文本真相、媒体只增不改。顶尖外部创作模型优先，代理零费用、不用用户凭据。本地占位预演不冒充商业生成。
不引入内部LLM、新MCP、React/Electron或远端UI。报告不是生产输入，不自动上传、付款、选型、选片或锁片。本地取消不意味着云端停止或不计费。
R2保存安全、R3取消与完成候选保护保持。旧PROJECT_STATE/TASK/ZIP和LAST_GREEN原样。只有文件存在和独立恢复核验后才给下载链接，不保证永久托管或用户设备落盘。

## 实测边界与索引
精确终态、原始红项、超时及独立安装结果见 `REPORTS/continuation/R14_VALIDATION.md` 与最终EVIDENCE，重复执行不累加。无商业生成和真实画质盲测。
Linux/Python3.13.5/Chromium144/FFmpeg7.1.5；isolated_document不是Windows原生双击。全回归、Windows实机安装/退出/升级回滚、固定FFmpeg6.1.1和商业执行仍为未关闭门，LAST_GREEN不推进。
原上限128MiB单文件、512MiB去重媒体、16导演锚点及PNG边界保持。总备份不含整个影片工程/外部剪辑项目/未绑定文件；命名副本不是长期分支数据库或同步。

- `TASK_R14_组合与模板边界_2026-09-12_02-35-17.md`：组合/模板/引用闭包/预览/身份稳定性要点。
- `REPORTS/continuation/R14_PROJECT_REVIEW.md`：源码支持的欠缺与改进范围。
- `REPORTS/continuation/R14_VALIDATION.md`：实际回归及媒体演练边界。
- `tools/delivery/QUICKSTART_ZH.md`：个人入口、备份回验和CLI。
- `PROJECT_STATE_2026-09-08_13-51-38.md`及其索引：历史背景保留。
- 最终PACKAGE、SHA256SUMS与独立receipt：身份/完整性，不替代测试或落盘证据。

## 假设与未决
假设：局部取用和方法模板减少重复操作；没有用户长期亲测，不声称百分比收益。
组合是工作区整块替换，不做数组/批准记录深合并。个人模板只编辑声明的文字字段，不能绕过新型号证据和人工裁决。
Windows实机、大媒体分块、多项目同时标签页、跨设备同步和真实商业执行未在本轮闭环。

## 变更日志
2026-09-12_02-35-17 UTC：记录R14组合与模板决定。精确终态和下载身份以独立证据为准，不复写旧快照。
