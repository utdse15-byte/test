# PROJECT_STATE

快照：2026-09-12_14-37-01 UTC。仅时间戳最新一份有效，旧快照保留。

## 目标与阶段
用户要真正可下载、可解压、可继续使用的完整产品，愿意使用外部顶尖工具，强调质量优先和灵活进出。本轮基于已验证 r16.3完整包（SHA-256 e79ef20b4483e7e6f8fea9c9499a3e6fcab743faade2397d626e1c7a0d9e8524，Git 55990e7408e22679d265b4693ad13e4f5eb89d2a）进入维护版0.2.0+r16.4。R17原包未恢复，R3/R4是功能重建。不以版本号代替交付；最终提交、包哈希和发布成功只认实际PACKAGE.json及当轮收据。

## 有效决定与理由
新增可选本机续作只读内容摘要：主动读取整点SHA-256后只解析有上限的文字元数据，不提取媒体。展示镜头/方案、提示词摘录、素材/审片数量、返工/导演及待处理材料；筛选仅查已读短摘要。缓存仅在当前页内存，重新点击会重新读盘，坏点保留且不阻断其余点。保留旧POINT.json/Desk/Studio格式，不新增磁盘索引。
本窗自动保留点使用单调保存序号，不用系统时间判断新旧。新点完整发布成功后才清理本窗旧点；不清理其他窗口。
动态恢复列表由显式区域显示/收起，避免演练中原生details处于open却内容不可见；键盘可用。读取期间发现新输入，完成时显示尚未保存，不能沿用旧成功状态。
原素材重连、完整响应超时、独立备份来源分离继续保留。可选服务仍环回随机端口/路径/令牌，严格Host/Origin，固定路由、标准库、不公开网络。普通HTML入口不要求Python，不强迫本机续作。原工程与旧包不覆盖。

## 长期硬约束与偏好
只用顶尖模型，不自动降档、付款、上传、生成、选片或锁片。原十二档与质量名单不变，本轮未重排。远程计费不能由本地取消推断。LAST_GREEN不推进。
单素材128MiB、去重媒体512MiB、16导演锚点、重连一次最多256文件不变。单恢复点520MiB；默认恢复目录2GiB/64份，每窗保留3份。停笔约8秒、相邻自动保存至少30秒，不保证关闭页面瞬间保存。新写入失败不先删旧点换空间。
Desk覆盖三个工作区、已载入未接受改稿/模板和相关文字；不含主影片目录、未绑定文件、未应用能力档或组合预览、临时撤回、播放器位置。不加密/签名、不云同步。用户电脑落盘须选回文件核验。摘要每段240字符，名称160字符，停止只阻止后续摘要；当前读取完成或超时后结束。

## 本轮实证与遗留
终态选定 509 项同进程完成，508通过、1跳过，43项新增包含其中；重复运行不累加。六组源码/独立安装版实际媒体演练通过：五份素材、1920×1080PNG保真、两视频完整解码、重启检索正确点、新稿不改、原r16.3页面回程ZIP逐字节一致。时钟倒拨原版保留1/2/5，修复后3/4/5。
本轮原生file导航被策略拒绝，使用原HTML隔离文档和真实HTTP测试传输桥，不修改生产源检查。首次状态提示、测试脚本变量冲突、details可见性失败均保留。全项目回归、Ruff、Windows实机、最大容量/断电/多浏览器、固定FFmpeg未验收。未重跑六秒渲染，不能引用旧轮结果冒充新测。

## 任务索引
- TASK_恢复点索引_2026-09-12_14-37-01.md：摘要/保留顺序/界面边界、复现和后续验收。
- REPORTS/recovery-index/VALIDATION.md 与 PROJECT_REVIEW.md。
- tools/delivery/LOCAL_CONTINUE.py、CONTINUE_CLIENT.js。
- tools/user_ready/rehearsal_recovery_index.py、rehearsal_downloads.py 与 build_continue_delivery.py（MANJU_RECOVER新产物）。
- 原本机续作、素材找回和更早任务档案均保留在源码根目录。

## 假设
用户可能有Python3.11+，未在其设备验证。摘要整点哈希对大素材有成本，最大额定容量未实测。不把只读描述当完整媒体/语义核验或备份健康证明。

## 变更日志
2026-09-12_14-37-01：增加只读恢复内容检索，修复时钟倒拨保留顺序、动态列表可见性和忙时新输入提示。其余创作核心与原格式保留。
