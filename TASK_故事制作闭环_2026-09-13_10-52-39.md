# 故事制作闭环任务快照

阶段：r16.8候选，基于实际r16.7，源文件与终态试验已完成；最终交付看PACKAGE和外层审计。

选择做工作副本而非第二个正典Bible。先按真实场序和明确知识范围取得上下文，再冻结含内容的简报。Sources和Scenes的稳定ID保留；改名不拆身份，但被引用内容（含名字）改变需复核。人物知情每条资料显式声明一个人物；多人共同知道时需作者明确维护记录，不自动推断。

Compiled快照固定order_prefix和选择过的前情/引用，非作者不带ending；角色视角不自动带前情正文。源内容隐藏时只保留ID与排除原因，不泄露标题/正文，但ID和自由文本本身需作者检查。原始Scene和references作为当时的任务内容保留。观察记录不参与上游有效性判断，也不意味着选片批准。

旧源目录系列读入来自core.Series注册/验证后的共享Bible，输出作者资料；出处是相对文件、稳定键及entry/file哈希。不是实时链接、不是幕场切分和自动剧本同步。用户后改原Bible应重新只读导入复核。

Desk/v2仍是DESK.json/STUDIO.zip/MANIFEST.json；Python parse_desk分流，原Desk类v1保持。新工作本可单独导出、预览完整替换，也可随Desk保存。旧Studio没有新增story字段；不要让旧v1保存按钮冒充完整故事备份。v2外层旧软件不兼容，旧内层回程已真实验过。

当前实际媒体只来自studioBindings，历史brief本身不会保持被其他区全部移除的文件。外发缺文件必须拒绝，不能用hash占位造空壳。关联当前镜头必须在state.assets和files里找到真正引用，不能假称别的区有视频就给供应商了。

重要遗留：尚无故事JSON逐字段三方合并、图形化关系网、跨设备协作、自动库同步或不限容量。草稿自由文本中的剧透/错误由作者负责，不能宣传自动理解剧情。必须继续保留质量名单、显式确认与不自动重生成。

终态937测试：936通过1跳过，另外18新发布门测试；源/安装含实素材演练。旧失败日志保留。测试使用原HTML隔离文档不是Windows原生。独立新简报ID不同是正常，应比较内容哈希和素材而非强制所有独立总备份相等；各自保存重开须字节一致。

交付必须：干净Git、当前源指纹、完整源码wheel与266文件一致、原R2补丁可重放、五传输片段合成同一ZIP，再对最终wheel实际安装重跑故事操作。旧档案不覆盖，LAST_GREEN不推进。最终成功只能记录在真实生成后的下载审计。
