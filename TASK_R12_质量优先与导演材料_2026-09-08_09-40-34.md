# TASK：R12质量优先与导演材料

定稿：2026-09-08T09:40:34.348051+00:00。此快照不原地更新。

## 已确认决定
用户只接受顶尖模型，不以省钱为理由降档；本轮执行继续零付费。质量名单不是执行provider，不改变本地占位预演。兼容目录保留旧档恢复，质量覆盖层绑定具体档内容哈希。
Wan3标准版、fal后训练H3 Max是新增档；Google Omni1.1的依据是官方继任而不是旧榜单的同端点独立分数。未测真实商业题材，不发布万能冠军。原十档对象不变。

## 精确契约与约束
导演/v1是独立闭合storedZIP，不是Request/Workspace/Repair v1新字段。三个区域要分别保存。固定 source_role=motion_timing_camera_reference_not_final_look、delivery_status=draft_reference_only、execution_authorized=false、automatic_selection=false。
每个锚点是相对源片播放毫秒，唯一递增且小于片长；帧PNG与源片显示像素相同，目标PNG仅要求同画幅，允许更大分辨率。不得在恢复/封包时为“匹配”缩小目标图或改原片。缺图/缺文字可以保存，但不是准备完成或已批准。
原帧为浏览器SDR参考，不可写成HDR/EXR或帧精确母版。CLI独立校验哈希与有界PNG结构，但不全解码或重探视频；浏览器恢复另行全解码PNG并重新测实际源片时长/尺寸。
闭合ZIP拒绝重复/额外/缺少路径、压缩/加密/链接等不支持布局；哈希不是发布者签名或身份/版权证明。资源上限是本地防护，不得借用为模型参数。
质量导出是说明性证据，不是人工批准。过期名单告警、不降档，未来日期不生效；能力档同ID变内容必须重新核查质量依据。

## 实际证明与未完成
532选定节点531过1跳；59新增全过。源码/独立安装8组实际演练通过；导演ZIP与六秒本地占位成片跨版字节一致。全6854仅收集，Hypothesis三个模块失败，Ruff未跑；Windows实机及固定FFmpeg未验。原始失败和未完成运行日志保留，不合并成“完整全绿”。
运行时/目录/HTML/版本274项与受测wheel一致，旧10档/173CLI/15状态任务绿灯保持。最终ZIP与Git恢复收据独立验证才是交付，不以本快照替代。

## 后续不应重复或误做
不要重新造同名H3Max或Google档而混淆入口，不用Flash字样推断质量，不把榜单几分差距当统计确定性。不要移植ChatCut低成本策略或Comfy旧模型示例。本轮采用的是角色分工、运动先行和可恢复过程。
不要未经新格式设计把导演/返工区塞进旧完整现场ZIP，导致旧批准/哈希漂移。高精度帧导出、目标模型实际关键帧提交、画质盲测仍未实现；未来做这些须保留源时基/色彩和任务执行证据，不能用现有SDR页面截图代替。
