schema: manju.canary-fixture/v1
fixture_id: canary.tts.v1
expected_capability: tts
max_estimated_cost: 0.05
# short public-domain text (§5) — no private voiceprint / restricted script (test 18)
prompt: "Manju canary check. One two three."
duration_ms: 300
expected_duration_range_ms: [100, 1500]
required_receipt_fields: [job_id]
forbidden_secret_fields: [authorization, api_key, apikey, token, secret, bearer, key, password, credential, signature]
sample_artifact: canary.audio.wav
