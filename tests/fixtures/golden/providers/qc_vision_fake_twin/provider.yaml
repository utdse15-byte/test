# AI_IDE_20A golden-corpus fake qc_vision reviewer (TWIN / disagreeing).
#
# The second opinion (contract §5: disagreement rate). Same vision slot shape as
# qc_vision_fake but a DISTINCT id, capabilities and review_profile → a distinct
# profile_digest, and a lookup table with deliberate conflicts on declared cases
# (see manifest.json cases[].reviewer.twin). Used by AI_IDE_15's multi-reviewer
# and disagreement-rate tests. A pure TEST DOUBLE — no network, no model.
id: qc_vision_fake_twin
type: vision
adapter: manju_test_fake_vision
capabilities: [visual_review, consistency_screening]
disabled: false
review_profile: twin
cost:
  per_call: 0.0
  currency: CNY
