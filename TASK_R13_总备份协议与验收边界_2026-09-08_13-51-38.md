# TASK R13：总备份协议与验收边界

定稿快照：2026-09-08T13:51:38.213566+00:00 UTC。后续不可修改此文件。

## 已确认的决定
R13是实际R12的累计代码，不是重新从R2丢掉R8-R12。旧两个模型目录文件、旧协议和黄金批准意义保持。
Studio/v1是另一个组合协议：嵌入原Workspace/v1，加raw repair/director、auxiliary_form和全场去重media。不是把旧Workspace/v1偷偷扩容。两侧严格拒绝重复JSON键、隐式转换、超界、重复或未引用ZIP成员、压缩/前后缀/目录矛盾/篡改。
auxiliary_form保留return-width、return-height、promotion-resolution、asset-role四个原始字符串。临时审批/型号选择、未应用catalog预览、派生报告和播放位置不保存；恢复质量优先true、当前确认false。
草稿必须允许尚未完成的业务字段，不能用执行级规范化去修剪或拒绝原始写作。实际引用媒体仍必须完整且哈希一致，不接纳无底片的导演锚点。高像素guide不缩小。
返工上下文request仅作语义上下文，旧返工区原本不绑定其全部外部素材；不要错误宣称总备份能取得从未绑定的其他文件。真实影片工程不在Studio中。

## 代码属主与重建
Python：src/manju/authoring/studio.py。浏览器：tools/workbench.studio.html/js，经tools/rebuilt-delivery/build_workbench.py读取DELIVERY_VERSION重建两份相同HTML。不要只编辑生成的HTML。
shared handled追踪所有异步UI操作，Studio导出/导入/应用拒绝与未完成旧操作交错。确认之前的所有验证在隔离对象上进行，应用段无异步等待，避免半更新。
选回已存ZIP是只读核验，不是恢复。未与当前studioView一致不得清除备份警告；局部JSON不能替整个现场“代存”。浏览器不能证明用户永久保存了文件。
CLI studio-verify成功stdout JSON，失败沿用该命令族stderr文字/退出码2，不擅自改旧CLI错误协议。Python只验结构/哈希/PNG结构，不能升级声称完整视频解码。

## 最终证据与局限
单进程632节点631通过1跳过，65新增全部通过；8组源码/独立wheel真媒体演练通过。详见R13_VALIDATION与EVIDENCE/FINAL_SELECTED_TESTS.json、FINAL_SOURCE_INSTALLED_PARITY.json。重复/未完成运行不得再计数。
三缺Hypothesis模块、Ruff、固定FFmpeg、Windows实机及商用模型执行仍未闭环。旧R3/R4原ZIP从未找回；LAST_GREEN不能因R13局部绿灯推进。

## 后续接手必须避免
不要把总备份当整台影片应用工程备份，或将恢复历史当新批准；不要按当前型号名字自动修改R12质量证据；不要将原生file被阻止的isolated_document说成Windows通过；不要在没有最终ZIP及独立收据的情况下发布链接。
需要真实Windows矩阵、依赖完整环境和用户实际题材的质量评测；当前没有凭据、没有已付费生成结果。只以获得的实测继续，不依据版本名推断功能或数据存在。
