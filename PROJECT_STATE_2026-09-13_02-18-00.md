# PROJECT_STATE

快照：2026-09-13_02-18-00 UTC。研究日期为用户当地2026-09-12。全局仅时间戳最新一份有效，旧快照保留。

## 核心目标与阶段
用户需要真正能下载、解压、继续工作的完整产品；常使用外部顶尖工具，只接受质量优先、不自动降档。当前维护版0.2.0+r16.5基于真实可恢复r16.4：Git 1f462282578500c641cf5101d11ab730ea1e15f2，原完整包SHA-256 fddf7e0f508da75b4c176c655bbbb848238d08896373419ad1ca4b0559cc0e6d。R3/R4是功能重建，原R17没有恢复。提交和文件交付结果只认最终PACKAGE.json和实际核验收据，源码状态不预写成功。

## 本轮有效决定与理由
重点从下载/备份按钮转回外部创作往返：在已有导演区增加逐关键帧精修任务，原帧/旧目标图按字节导出，每张独立提示词和原计划哈希绑定的返回名；不包含底片视频，故不是完整备份。
两个明确手动入口：自行选择顶尖工具、OpenAI固定快照gpt-image-2.5-sunburst-2026-09-08并请求max质量。核验日2026-09-12，复核日2026-09-19。官方定位不是独立盲测；未调用商业API。现有12个视频能力档与质量名单逐字节保留，未重新排名。
TASK.json及路线快照不可改写。返回可分批，核对共用底片/镜头/保留改变要求和每个返回锚点的时刻、原帧、目标与旧图。无关锚点可编辑；已改目标图拒绝覆盖；重复相同像素文件是无操作。逐批全部验证成功再应用，预览后编辑即失效。只绑定未批准目标图，其他区域文字/原片不动；当前确认清除，质量优先开启。
返回只接受实际浏览器可解码的同画幅PNG，可以高于底片像素，原字节保留。不缩图/裁切/上采样。原帧仍是SDR参考，毫秒不是认证帧编号；不认证C2PA、模型来源、版权或画质。
新任务JSON有首页入口识别；新CLI仅做结构/哈希核验。已应用目标图继续保存在原Director/Studio/Desk协议中；未应用精修TASK/PNG/预览要独立保留，不偷偷改变旧备份协议。
未来改模型资料必须保留这条具体快照路线，不在原ID内更新参数导致历史任务暗中失效。新任务协议manju.retouch-task/v1已登记CONTRACTS。

## 长期硬约束与确认偏好
不自动上传、付费、生成、弱模型回退、选片或锁片；云端计费不能从本地取消推断。LAST_GREEN不推进。旧原包、真实影片工程和历史状态不覆盖。
普通HTML不要求Python；可选本机续作Python3.11+只用标准库、环回随机路径/令牌、固定路由/严格Host与Origin。续作不是独立备份或云同步。
单素材128MiB、去重媒体512MiB、16导演锚点、批量重连256文件等旧上限不变。导演PNG沿用32MiB、8192边长和像素数限制。恢复点520MiB，默认目录2GiB/64点，每窗按保存序号保留3份，新点成功才清理旧点；停笔8秒、相邻自动保存至少30秒，不保证关窗最后时刻。
Desk覆盖三个工作区、已载入未接受外部改稿/模板和相关文字；不含整部影片目录、未绑定文件、待应用能力档/组合/精修任务、临时撤回或播放器位置。附件非永久托管；用户本地字节须选回核验。内容哈希不是签名身份认证。

## 本轮实证与遗留
终态选定713个测试点同进程完成：712通过、1跳过，含39项新增。源码和独立安装265个运行时文件一致；两版精修任务ZIP、接回后的导演ZIP和收工ZIP逐字节一致。2秒640×360合成视频配1920×1080目标PNG，分批接回保真，其他镜头不改；原r16.4页面导入/播放/再导出一致。均为合成素材，不是AI画质实测。
原生file://被策略拒绝，成功浏览器采用原HTML隔离文档，不等于Windows双击。首次失败来自测试CSP表达式/重复幂等夹具及主镜头准备错误，日志保留。全项目回归、Windows安装/退出/回滚、Ruff、固定FFmpeg、最大额定容量未验收；本轮没有重跑六秒渲染，不能复述旧轮为新证据。
最终封包门要求回归rc/XML、源/安装真实结果及受测文件指纹；最终ZIP解压、真实分段下载与最终安装演练另以实际收据确认。

## 任务索引
- TASK_关键帧精修_2026-09-13_02-18-00.md：上下文绑定、版本化路线与测试边界。
- REPORTS/retouch/RESEARCH.md、PROJECT_REVIEW.md、VALIDATION.md、README.md。
- src/manju/authoring/retouch.py；tools/workbench.retouch.js/html；image_routes.json。
- tools/user_ready/build_retouch_delivery.py、rehearsal_retouch.py；通用rehearsal_downloads.py保留旧默认且支持--prefix。
- 上轮恢复摘要、素材重连、本机续作和更早档案均原样保留。

## 假设
用户可在自己的顶尖图像工具输出PNG并按指定名字保存，未在用户账号或设备验证。某平台不可选固定快照或max时，需要用户重新明确选择入口，不应自动等价替换。官方发布文档可变化，复核日期是提醒，不等于长期保证。

## 变更日志
2026-09-13_02-18-00：加入具体入口精修材料包和分批返回、防错上下文、只读核验命令，维持质量偏好与旧协议，新增当前交付器与真实媒体验证。
