# TASK — Research / Dogfood R2

定稿：2026-09-06 09:44:32 UTC。定稿后不改写。

## 目标与身份

用户授权研究快速变化的模型/同类应用，深度使用当前项目并直接修 bug；当时尚未严格个人实测，未给出其本机具体报错。不能声称已经修好用户那台机器的未知问题。
输入原 ZIP `MANJU_ONE_PRODUCT_POLISH_R1_FINAL_ee1e524_2026-08-15.zip`。
基线 `ee1e524a8640a121101603d3da9a2acb78a49c50`；运行时代码 `62ccbb96894ee7a9c22c1329b2d4f0cbd5524515`，src 树 `9bfc068654d5534642e2ca33b29c6548f6832e16`。最终交付 HEAD/恢复结果看包顶层 manifest，不猜自引用 SHA。

## 已落地、不要重新造轮子

原生 inert 行模板修首条 SFX/信息卡，不复制上一行已填写值。
故事保存进入原共享锁/CAS/原子写路径，但未完成草稿不要求工程 schema 通过；GUI 必带所显示版本。历史 API 不传 expected_rev 仍按旧约定写入，任意外部编辑器不遵守锁时的极小竞态没有被证明消除。
浏览器只标记提交快照已保存，刷新时重新检查各阶段草稿/进行中的保存。
R3 渲染器 `2026-09-06.r1` 新增完整性绑定的 `ASSET_MAP.md`；实体文件与多主体逻辑绑定分开。只修新包上传扩展名，旧渲染器旧命名不原地改写。
纯说明模块在 conform 中明确非时间线目标；安全下载测试修真实 opener seam，不改产品网络策略。

## 已有验收 / 证据索引

最终可用范围 6403 项：6366 通过、15 失败、22 跳过，另有3模块缺Hypothesis未收集。新增30用例，相关174项通过，不能累加覆盖率。
证据目录：baseline-suite（原版）、final-suite（首轮改版）、final-available-suite（最终运行时）；regression-summary/comparison JSON 为统计权威。
composed-delivery：24视口、空表新增删除重建、真实409与草稿保留、F1/CtrlK、2秒实际素材回收。
edit-export-delivery：6秒三镜头，显式测试夹具选片，无生成，字幕/音效、缓存、混音重渲染、老文件哈希、真实导出/解析和完整解码。
historical-handoffs：真正 `2484c65` R1 和原包 R2 源码各创建两配置包，最终代码4/4验证且字节不变。
final-wheel：SHA99308469698b9b3413b1767f08eb06f568e1ce27ec72451ebbd8802ec09b838a，240包文件匹配独立安装。

## 未决事项与已排查边界

最终失败均在原包基线中出现；这不等于完整发布已通过。
12个用例涉及原 Chromium 文件截图路径超时/降级；2个内容QC用例未显式选片即要求成功；1个GUI本地取消30秒不达标。精确最终ID见验证报告。
不要把失败说成全是环境，尤其不要用远端语义 ProviderCanceled/fake job ID 修本地取消。应从取消 token 到本地子进程生命周期统一闭环，再证明远端任务/计费语义不被混淆。
首轮真实SIGINT曾在2.5秒发信号前以人工选片门exit1结束；原/新单跑均130通过。假设：固定sleep的测试存在时序敏感；未据此改测试或宣称消除风险。
测试 conftest 会清除全局执行模式，不得将两条内容QC失败归因为 strict 专属。
浏览器localhost被策略拦截，组合测试不是真实Origin/CSP/Windows E2E。较早桌面首页截图是加载占位，工具改成等真实主卡后重跑，最终home_ready=true。
早期工具误断言帮助文案、异步关闭回调和一次工具超时均留历史，不算产品bug或最终成功。

## 下一轮最高杠杆

先完成真实Windows个人操作/应用生命周期及本地取消语义闭环，再在固定FFmpeg6.1.1、依赖齐备环境做同SHA双平台门；没有预算与凭据条件不要调用真实提供商。
最新模型研究12个官方来源截至2026-09-06；Sora计划9月24日退役不是当日已停服，H3/Max输入模式不同。配置还是离线作者交接，不是原生API认证，旧冻结配置禁止无版本覆盖。
不要恢复MCP/内部LLM、重做GUI平台、放松人工选片或大拆核心模块。用户原项目媒体不在源码包中。
