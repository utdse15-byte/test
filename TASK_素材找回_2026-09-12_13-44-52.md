# 任务：原素材找回与可核验交付

基线完整 r16.2，最新全局状态 PROJECT_STATE_2026-09-12_13-44-52.md。新增tools/workbench.relink.*，在Desk之后构建到两份同字节workbench.html；各区域原引用和File绑定分离，预览由deskGuard+绑定对象身份约束，接回前再SHA-256读取，全验证后同步提交。当前只修缺失，现有错误绑定不在本轮自动替换。

测试 test_user_media_relink.py 21项，test_continuation_response_deadline.py 2项。完整选定466项见EVIDENCE/final-regression.xml。rehearsal_relink.py使用五份已有合成媒体，移除绑定是显式测试故障注入；旧版再导出与输入同字节。rehearsal_continue.py复核真实服务重启。

运行脚本需PYTHONPATH=src（源码）或独立安装目录，浏览器MANJU_BROWSER_TEST_TRANSPORT=isolated_document，不能因此声称原生file/Windows通过。测试首轮出现CSP字符串等待和不正确预期；修测试而不削弱CSP。实际播放检查改为等currentTime推进，而不是固定睡眠猜测。

构建使用tools/user_ready/build_continue_delivery.py：要求空目标目录、终态rc=0、tested-source-hashes.json、四组RESULT以及源码安装版同导出哈希。最终core经源码/wheel/hash/Git/patch核验后排他发布。输出MANJU_RELINK_FULL、START、助手和五段。不得覆盖以前CONTINUE系列。

未完成：整个项目全量回归、Windows和最大容量矩阵；没有新增商业执行或模型排名。不要将清单、重连或本机恢复点解释成独立备份。
